# v0.16.0 — Mission-Driven Dashboard Progress + Program Content Management — Test Results (D-069)

Code version: `v0.16.0`

Test date: `2026-07-07`

Environment: local Docker Compose stack (Postgres, Keycloak, MinIO, applicant `:3100`,
admin `:3200`, both rebuilt from this branch), Windows 11, browser checks via Playwright driving
Microsoft Edge.

## Automated checks

| Check | Command | Result |
| --- | --- | --- |
| Typecheck | `npm run typecheck` (root + per-app configs) | Pass |
| Lint | `npm run lint` | Pass (0 warnings) |
| Unit tests | `npm test` | **202/202 passed** (30 files; new: `program-content.test.ts` CRUD/audit/tenant-scoping suite, `getApplicantMissionProgress` suite in `submissions.test.ts`, `manageProgramContent` capability matrix in `permissions.test.ts`) |
| Dashboard regression area | `npm run regression:dashboard` | **3/3 passed**, incl. new "Accepted mission submission moves mission-driven dashboard progress" (draft→submit→accept loop against marked fixtures) |
| Programs regression area | `npm run regression:programs` | **2/2 passed**, incl. new "Org Admin manages program content; roles without manageProgramContent are denied" (capability matrix, CRUD round-trip, audit assertions, cross-tenant delete rejection) |

No migration or seed change in this version (models exist since `v0.12.0`).

## Browser (end-to-end) verification

Playwright + Microsoft Edge against the rebuilt containers, logging in through the real Keycloak
OIDC flow as `orgadmin@demo.talentos.local` (admin `:3200`) and `accepted@demo.talentos.local`
(applicant `:3100`), exercising the full v0.16.0 loop in one session:

| Scenario | Result |
| --- | --- |
| Admin: program detail shows "Manage content →"; content page reachable | Pass |
| Admin: create video resource, weekly task and calendar event via the new forms | Pass |
| Applicant: dashboard shows Current Mission card and Missions Accepted tile; baseline 0% | Pass |
| Applicant: admin-created resource/task/event visible on Resources, Tasks and Calendar pages | Pass |
| Applicant: save draft + submit Week 1 mission; Current Mission chip reads "Submitted — awaiting review"; progress still 0% (submitted ≠ accepted) | Pass |
| Admin: submission listed on the mission page; "Accept submission" succeeds | Pass |
| Applicant: dashboard moves — Overall Progress 25%, Missions Accepted 1 of 4, Week 1 bar 1/1 · 100%, Current Mission advances to the Week 2 mission ("Not started") | Pass |
| Admin: delete all three content rows; content page and applicant views no longer show them | Pass |

**19/19 checks passed** in one continuous two-actor session (screenshot of the moved dashboard
captured at 25%).

## Audit-trail verification

`audit_logs` after the run (newest first) shows every write attributed and typed as designed:
`event.deleted` / `task.deleted` / `resource.deleted` (cleanup), `submission.reviewed` (accept),
`submission.submitted` / `submission.updated` / `submission.created` (applicant loop),
`event.created` / `task.created` / `resource.created` (admin content forms).

Demo data was restored after verification (e2e submission, notifications and content rows
removed), so `accepted@demo.talentos.local` is back at a clean baseline.

## Notes

- The one-shot e2e harness initially flagged two false failures caused by asserting on a single
  dashboard snapshot before the server-action revalidation landed; re-checked with short retries,
  every scenario passes and the database confirmed each write at the moment of the "failure".
  No product defect.
