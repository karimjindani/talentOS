# v0.11.0 — Org-Admin Auto-Provisioning — Test Results

Code version: `v0.11.0` · Date: `2026-07-02`

## Automated checks

| Command | Result |
|---|---|
| `npm run db:generate` | Passed |
| `npm run typecheck` | Passed (root + applicant + admin + ops) |
| `npm run lint` | Passed (both apps, `--max-warnings=0`) |
| `npm run test` | **71 tests passed** (65 prior + 6 new `keycloak-admin.test.ts`) |

New file `apps/admin/lib/keycloak-admin.test.ts` (6 tests): `adminBaseFromIssuer` strips the realm path;
`generateTempPassword` satisfies the realm policy (≥12, upper/lower/digit/special) and is unique;
`provisionOrgAdmin` (fetch-mocked) creates a new user with `emailVerified` + required actions + temp
password + `ORG_ADMIN`, and is idempotent for an existing user (no password, role ensured); the realm
import declares `talentos-provisioner` with `serviceAccountsEnabled` + `manage-users`.

## Live end-to-end (running Keycloak)

Live-patched the running realm to add `talentos-provisioner` and grant its service account
`manage-users`/`view-realm`/`query-users`, rebuilt `admin`, then exercised `provisionOrgAdmin`:

| Case | Result |
|---|---|
| New admin `founder@zeta.test` | `created=true`, temp password returned, Keycloak user id assigned |
| — user rep | `emailVerified=true`, `enabled=true`, `requiredActions=[CONFIGURE_TOTP, UPDATE_PASSWORD]` |
| — effective realm roles | includes **`ORG_ADMIN`** ✅ |
| Existing `owner@acme.test` (re-provision) | `created=false`, `tempPassword=null`, same user id (idempotent) ✅ |

The service-account token (`client_credentials` for `talentos-provisioner`) authenticated and performed
user create + password set + role mapping. Test user `founder@zeta.test` was deleted afterward.

## Deployment

`docker compose up -d --build admin` rebuilt and started cleanly (exit 0); admin portal `307` → Keycloak
sign-in. Realm client added to the import (fresh envs) and applied live via `kcadm.sh` (running env). No
DB migration.

## Manual verification (recommended follow-up in a browser)

1. Sign in to the admin portal as `superadmin`, open `/organizations`, create an organization with a new
   org-admin email → the page shows the **one-time temporary password**.
2. Sign in to the admin portal at that tenant's subdomain as the new org admin using the temp password →
   Keycloak forces a password reset + TOTP setup, then the admin reaches their tenant's admin portal
   (realm role gates entry; DB membership scopes authority; `keycloakSubjectId` links on first login).

## Notes

- No DB schema change. Regression suite: 71 tests.
- Authentication uses the least-privilege `talentos-provisioner` service account — no master-admin
  credentials in app config.
