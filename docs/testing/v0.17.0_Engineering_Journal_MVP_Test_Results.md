# v0.17.0 Engineering Journal MVP Test Results

Date: 2026-07-07

## Summary

Engineering Journal MVP verification passed for schema validation, Prisma client generation, unit
tests, TypeScript, lint and production build. The new migration exists and is pending locally until
applied to the database.

## Commands

| Check | Result |
| --- | --- |
| `npx prisma format --schema packages/db/prisma/schema.prisma` | Passed |
| `npx prisma validate --schema packages/db/prisma/schema.prisma` | Passed |
| `npm run db:generate` | Passed after elevated rerun due Windows `EPERM` on generated client files |
| `npx vitest run packages/db/src/journal.test.ts apps/applicant/components/ApplicantShell.test.ts` | Passed, 21 tests |
| `npm test` | Passed, 212 tests |
| `npm run typecheck` | Passed |
| `npm run lint` | Passed |
| `npm run build` | Passed |
| `npx prisma migrate status --schema packages/db/prisma/schema.prisma` | Completed after elevated rerun; reports `20260707190000_v0_17_0_engineering_journal_mvp` pending |

## Notes

- AI scoring fields are schema placeholders only; no real AI scoring or AI review workflow is active.
- The migration should be applied locally with `npm run db:migrate` before using the journal pages
  against a local database.
- Initial non-elevated Prisma/Vitest commands hit Windows `EPERM` process/file replacement errors;
  elevated reruns passed.
- One build rerun hit a transient Windows/Next trace-file `ENOENT` after both apps compiled; rerunning
  `npm run build` by itself passed.
