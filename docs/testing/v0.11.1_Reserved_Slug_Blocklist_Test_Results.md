# v0.11.1 — Reserved Tenant-Slug Blocklist — Test Results

Code version: `v0.11.1` · Date: `2026-07-02`

## Automated checks

| Command | Result |
|---|---|
| `npm run db:generate` | Passed |
| `npm run typecheck` | Passed (root + applicant + admin + ops) |
| `npm run lint` | Passed (both apps, `--max-warnings=0`) |
| `npm run test` | **78 tests passed** |

Slug validation (`packages/auth/src/auth.test.ts`) now asserts: reserved slugs `www`/`admin`/`api`/`demo`/
`keycloak` → rejected; `acme`/`acme-corp` still accepted; reserved-substring slugs `acmeapi`/`admins`/
`my-demo` still accepted. (The 78 total also includes PR #13's `applications.test.ts` duplicate-application
tests, now present on `main`.)

## Database / migration parity

Applied the pending migration merged via PR #13 so the local DB matches `main`:

- `prisma migrate deploy` initially failed because the local DB held a leftover **duplicate active
  application** (`{ACCEPTED, SUBMITTED}` for one applicant+program) from earlier manual testing — a fresh
  seed is idempotent and does not produce this. Removed the stray `ACCEPTED` row, marked the failed
  migration rolled-back (`prisma migrate resolve --rolled-back`), and re-ran `migrate deploy` →
  **all migrations applied**.
- Confirmed the partial unique index `applications_applicantId_programId_active_key` exists on
  `applications` (`pg_indexes`).

## Manual sanity (recommended)

1. `/organizations` → create an org with slug `admin` or `demo` → rejected with the reserved-name message.
2. Create an org with a normal slug (e.g. `zeta`) → succeeds.
3. An applicant with a REJECTED application for a program may re-apply; a second *active* application for
   the same program is blocked (app-layer message + DB partial unique index).

## Notes

- Reserved-slug blocklist is validation-only (no schema change). The duplicate-application partial unique
  index is a schema addition delivered via PR #13, documented here and in the Data Model / Decision Log
  (D-054) for governance parity.
- Regression suite: 78 tests. Closes audit findings #1–#4.
