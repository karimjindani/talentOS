# v0.19.1 Dashboard Wiring & Same-Week Repeat Test Results

Date: 2026-07-14

## Summary

No schema change and no new migration. Verified the dashboard/program/tasks/missions wiring
manually in the browser as `accepted@demo.talentos.local`, and verified the same-week repeat
correction via a dedicated new unit test plus the existing repeat-loop regression scenarios. Full
gate and regression suite re-run clean.

## Gate Commands

| Check | Result |
| --- | --- |
| `npm run typecheck` | Passed |
| `npm run lint` | Passed |
| `npm test` | Passed (427 tests, 43 files) |
| `npm run build` | Passed |
| `npm run regression:all` | 35/36 passed, 0 failed, 1 pre-existing documented skip (storage) |

## Scenario Results

| Scenario | Automation | Result |
| --- | --- | --- |
| Dashboard Days Remaining reflects the real current-mission deadline | Deferred (manual browser check) | Verified |
| My Program shows real start/end dates and per-week completion | Deferred (manual browser check) | Verified |
| Deadline countdown appears only on the current, unsubmitted mission | Deferred (manual browser check) | Verified across Dashboard, Missions, Tasks, My Program |
| Rejection repeats the same week, not Week 1 | Added this iteration (`mission-assignments.test.ts`; `missions`-area regression scenarios) | Passed |
| No alternate mission for the failed week still notifies reviewers, scoped to the correct week | Deferred (`mission-assignments.test.ts` unit only) | Passed at unit level |

## Notes

- Deferred scenarios are recorded in `docs/Regression_Scenarios.md` Known Gaps.
- Live browser verification used hand-rolled cookie-jar/fetch scripts against the Keycloak OIDC
  flow (Playwright MCP unavailable in this environment).

## Verification

See `docs/plans/v0.19.1_Dashboard_Wiring_And_Same_Week_Repeat.md`.
