# v0.19.0 Mission-Driven Tasks & Submissions Admin Tab Test Results

Date: 2026-07-14

## Summary

Verified locally after applying migration `20260714110000_mission_tasks`. The fixed 3-task
template, the submission gate on Tasks 1/2 completion, the YouTube watch-gate parsing logic, and
the new admin Submissions tab were exercised at unit level and manually in the browser (dashboard,
tasks, mission and `/submissions` pages loaded and behaved as designed for
`accepted@demo.talentos.local` and `orgadmin@demo.talentos.local`). No dedicated scenario-level
regression automation was added this iteration — see Notes.

## Gate Commands

| Check | Result |
| --- | --- |
| `npm run typecheck` | Passed |
| `npm run lint` | Passed |
| `npm test` | Passed |
| `npm run build` | Passed |
| `npm run regression:all` | 35/36 passed, 0 failed, 1 pre-existing documented skip (storage) |

## Scenario Results

| Scenario | Automation | Result |
| --- | --- | --- |
| Submission is blocked until Tasks 1 and 2 are complete | Deferred (`mission-tasks.test.ts`, `submissions.test.ts`) | Passed at unit level; manually verified the Submit button is disabled until both tasks are checked |
| Task 2 requires watching the tutorial video to completion | Deferred (`youtube.test.ts`) | `parseYouTubeVideoId` passed at unit level; manually verified the "Mark as complete" gate in browser against a real YouTube embed |
| Task 2 has no watch-gate when the mission has no tutorial video | Deferred (`mission-tasks.test.ts`) | Passed at unit level |
| Only reviewers can see the admin Submissions tab's content | Already automated (`missions` area: "Only Org Admin and Tech Lead can review submissions") | Passed |
| `tutorialUrl` rejects a non-http(s) scheme | Deferred (manual security review) | Verified manually: a `javascript:` URI is rejected on write and not rendered as a link on read |

## Notes

- All Deferred scenarios are recorded in `docs/Regression_Scenarios.md` Known Gaps.
- Live browser verification used hand-rolled cookie-jar/fetch scripts against the Keycloak OIDC
  flow (Playwright MCP is unavailable in this environment — `EPERM` on `mkdir` for its browser
  cache directory).
- The legacy `ProgramTask`/`VideoResource`/`UserTaskCompletion` tables remain in the schema, unused,
  per explicit product decision; no migration drops them.

## Verification

See `docs/plans/v0.19.0_Mission_Driven_Tasks.md`.
