# v0.18.3 Ops Regression Scenario Visibility Test Results

Code version: `v0.18.3`

Date: `2026-07-11`

## Scope

Validation for the Ops Console change that preserves and renders individual regression scenario
results grouped by area.

## Commands

| Command | Result | Notes |
| --- | --- | --- |
| `npm.cmd run db:generate` | Passed after retry | First attempt hit a Windows Prisma DLL lock; local TalentOS Node processes were stopped and generation then passed. |
| `npm.cmd run db:migrate` | Passed | Applied `v0.17.0`, `v0.17.1` and `v0.18.0` migrations that were missing from the local database after pulling latest `main`. |
| `npm.cmd run typecheck` | Passed | Root, applicant, admin and ops TypeScript checks passed. |
| `npm.cmd run lint` | Passed | Applicant/admin ESLint passed. |
| `npm.cmd run test` | Passed | 245/245 tests passed across 34 files. |
| `npm.cmd exec -- vitest run apps/ops/src/jobs.test.ts apps/ops/src/server.test.ts` | Passed | 8/8 focused Ops parser/server/UI asset tests passed. |
| `npm.cmd run build` | Passed | Applicant and admin production builds completed. |
| `npm.cmd run regression:ops` | Passed | 2/2 Ops scenarios passed. |
| `npm.cmd run regression:all` | Passed after local DB migration | 25/28 scenarios passed, 0 failed, 3 documented skips. |
| `git diff --check` | Passed | No whitespace errors. |

## Scenario Results

| Scenario | Result | Notes |
| --- | --- | --- |
| Full regression emits detailed scenario results. | Passed | `REGRESSION_RESULT_JSON.results` includes every scenario with area, name, status, duration and optional detail/error. |
| Failed/skipped scenarios retain message text. | Passed | `apps/ops/src/jobs.test.ts` verifies detail/error preservation. |
| Existing summary-only payloads remain supported. | Passed | Existing fallback summary parser test remains passing. |
| Ops UI renders scenario-level cards. | Passed | `apps/ops/src/server.test.ts` verifies CSS/JS assets include scenario-row rendering support. |

## Regression Summary

Final `regression:all` summary:

```json
{
  "area": "all",
  "total": 28,
  "passed": 25,
  "failed": 0,
  "skipped": 3
}
```

Skipped scenarios:

- Tenant-scoped submission read rejects another tenant — skipped because only one tenant exists locally.
- Tenant-scoped program read rejects another tenant — skipped because only one tenant exists locally.
- Storage browser upload/download scenario — documented missing automation.

## Local Environment Notes

- The first `regression:all` attempt failed because the local database had not yet applied the
  `v0.17.0`/`v0.17.1`/`v0.18.0` migrations from the latest pull. After `npm.cmd run db:migrate`, the
  full suite passed.
- The Ops Console was running on `http://127.0.0.1:3300/` during `regression:ops`.
