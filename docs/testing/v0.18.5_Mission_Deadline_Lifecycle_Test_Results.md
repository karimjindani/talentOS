# v0.18.5 Mission Deadline & Lifecycle Test Results

Date: 2026-07-14

## Summary

Verified locally after applying migration `20260714090000_mission_deadlines_and_lifecycle` to the
local database. The mission lifecycle state machine (accept → deadline → grace → auto-advance →
reject-reassignment) is exercised by new and updated unit suites; the idempotent deadline sweep was
additionally run twice in succession against seeded fixture data to confirm no double-transition.
No dedicated `scripts/regression/run.ts` scenario yet drives these flows through the real
applicant/admin actions — see Notes and `docs/Regression_Scenarios.md` Known Gaps.

## Gate Commands

| Check | Result |
| --- | --- |
| `npm run typecheck` | Passed |
| `npm run lint` | Passed |
| `npm test` | Passed |
| `npm run build` | Passed |
| `npm run regression:all` | 35/36 passed, 0 failed, 1 pre-existing documented skip (storage) |

## Scenario Results

| Scenario | Automation | Result |
| --- | --- | --- |
| Explicit accept starts the deadline/grace clock | Deferred (`mission-assignments.test.ts`) | Passed at unit level |
| Idempotent deadline sweep marks a stale assignment `OVERDUE` | Deferred (`mission-deadlines.test.ts`) | Passed at unit level; re-run produces no duplicate transition |
| Grace-period expiry is terminal (`FAILED` + `DISQUALIFIED`) | Deferred (`mission-deadlines.test.ts`) | Passed at unit level |
| A late submission inside the grace period is still accepted | Deferred (`submissions.test.ts`) | Passed at unit level |
| Accepting a submission auto-advances the applicant, capped at the final week | Deferred (`submissions.test.ts`) | Passed at unit level |
| Rejection reassigns an alternate mission; no alternate notifies reviewers | Deferred (`mission-assignments.test.ts`) | Passed at unit level |
| A `FAILED` assignment cannot receive new evidence | Deferred (`submissions.test.ts`) | Passed at unit level |

## Notes

- All seven plan scenarios are recorded as Known Gaps in `docs/Regression_Scenarios.md` — they have
  strong unit coverage but no `scripts/regression/run.ts` scenario exercises the accept action, the
  sweep script, or the reject-reassignment path through the real applicant/admin flow yet.
- The recurring local-environment issue where the dev database's `_prisma_migrations` tracking
  table disappeared between sessions (while data tables persisted) recurred again during this
  iteration; resolved each time via `prisma migrate resolve --applied <name>` for prior migrations
  followed by `prisma migrate deploy`. Root cause still not determined — flagged for separate
  investigation if it keeps happening.
- The external sweep (`npm run mission-deadlines:sweep`) is not wired into any scheduler in this
  iteration; running it is a manual/cron-operator responsibility documented in `docs/Deployment.md`.

## Verification

See `docs/plans/v0.18.5_Mission_Deadline_Lifecycle.md`.
