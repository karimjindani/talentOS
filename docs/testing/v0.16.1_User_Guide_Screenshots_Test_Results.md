# v0.16.1 — Illustrated User Guide with Automated Screenshot Capture — Test Results

Code version: `v0.16.1`

Baseline commit: _set on merge_

Test date: `2026-07-07`

Environment: local Docker Compose stack freshly bootstrapped from `main` (`a95a42b`) via
`npm run local:bootstrap` (Postgres, Keycloak, MinIO, applicant `:3100`, admin `:3200`, Ops Console
`:3300`), Windows 11, Playwright 1.61.1 driving headless Chromium 149.

## Deployment verification (pre-capture)

| Check | Result |
| --- | --- |
| `git pull origin main` | Up to date at `a95a42b` |
| `docker compose up -d --build` (via bootstrap) | All 6 containers up; postgres/keycloak-postgres/minio healthy |
| `prisma migrate deploy` | 10 migrations found, **no pending migrations** |
| `db:seed` + `seed-dashboard` | Completed (9 tasks, 5 resources, 5 events, 3 notifications upserted) |
| Portal smoke (HTTP) | `lvh.me:3100/login`, `lvh.me:3200`, Keycloak realm discovery, Ops Console — all **200** |

## Regression safety (no product code changed)

| Check | Command | Result |
| --- | --- | --- |
| Unit tests | `npm test` | **202/202 passed** (30 files) — unchanged from `v0.16.0` |

`v0.16.1` adds only documentation (`docs/user-guide/`) and a standalone capture script
(`scripts/user-guide/`); no app, package, schema or configuration files were modified.

## Screenshot capture results

`npx tsx scripts/user-guide/capture-screenshots.ts` against the running stack:

| Section | Login flow exercised | Screenshots | Result |
| --- | --- | --- | --- |
| public | none (anonymous) | 01, 02 | Pass |
| apply | Keycloak **self-registration** of a fresh `guide.applicant.*@demo.talentos.local`, then real form submit with generated PDF CV | 03b, 04, 05 | Pass — `/application` shows `SUBMITTED` |
| dashboard | Keycloak OIDC login as `accepted@demo.talentos.local` (03 captured en route) | 03, 06–14 | Pass |
| admin | Keycloak OIDC login as `orgadmin@demo.talentos.local` | 15–22, 24, 25 | Pass |
| ops | Keycloak OIDC login as `orgadmin@demo.talentos.local` | 26 | Pass — "Ops controls are enabled" banner visible |

**26 screenshots captured; 1 documented skip** (`23-admin-submission-review.png` — the demo
mission detail page exposed no reviewable submission link at capture time; noted in the guide).

Spot verification: `04`, `05`, `06`, `16` and `26` opened and visually confirmed to show the
correct page, signed-in identity and expected data (e.g. dashboard progress tiles, applications
queue with status badges, ops controls enabled).

## Known byproduct

Each `apply`-section run leaves one Keycloak user and one `SUBMITTED` application in the `demo`
tenant (this run: "Guide Applicant"). Accepted as demo data.
