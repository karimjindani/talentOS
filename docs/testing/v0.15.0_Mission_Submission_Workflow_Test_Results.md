# v0.15.0 Mission Submission Workflow Test Results

Code version: `v0.15.0`

Test date: 2026-07-06

## Scope

Validates Mission Submission MVP-1 (D-067): applicant evidence submission (repository / deployment /
Loom URLs + Engineering Journal markdown), the staff review loop (accept / request changes →
resubmit), the `reviewSubmissions` capability boundary, tenant isolation, notifications, audit
logging and the schema migration.

## Unit / Static Validation

| Check | Command | Result |
| --- | --- | --- |
| Migration deploy | `npm.cmd run db:migrate` | PASS — `20260706090000_v0_15_0_mission_submissions` applied (10 migrations total) |
| Prisma client | `npm.cmd run db:generate` | PASS |
| Type safety | `npm.cmd run typecheck` | PASS (root + applicant + admin + ops) |
| Unit tests | `npm.cmd run test` | PASS — **187 tests** (161 → 187: +7 submission transition/editability, +3 `reviewSubmissions` matrix, +16 `submissions.ts` helper tests incl. URL allowlists, ownership, status guards, notification/audit writes) |
| Production build | `npm.cmd run build` | PASS (applicant + admin) |
| Lint | `npm.cmd run lint` | PASS (`--max-warnings=0`) |
| Compose config | `docker compose config --quiet` | PASS |
| Whitespace | `git diff --check` | PASS |

## Scenario Regression (`scripts/regression/run.ts`)

`npm.cmd run regression:missions`: **4/4 passed** — including the two new scenarios:

- *Submission loop: draft, submit, request changes, resubmit, accept — with notifications and audit*
  (also asserts ACCEPTED is terminal: evidence is no longer editable).
- *Only Org Admin and Tech Lead can review submissions* (HR and APPLICANT denied).

`npm.cmd run regression:all`: **19/20 passed, 0 failed, 1 skipped** (the pre-existing documented
storage-upload skip). Includes the new tenant-area scenario *Tenant-scoped submission read rejects
another tenant*. `npm.cmd run ops:cleanup-regression` removed all marker-tagged records
(`Submission` is now a tracked cleanup entity type).

## Browser End-to-End (Playwright/Chromium against the live Docker stack)

Full SEM loop on the seeded Week 1 mission (**Build a Public Product Landing Page**), demo tenant:

| # | Scenario | Result |
| --- | --- | --- |
| 1 | `accepted@demo` sees the **My Submission** section on the mission detail page | PASS |
| 2 | Draft (repo + deployment + Loom + journal) saves with confirmation | PASS |
| 3 | **Submit for review** locks the evidence (read-only, "Submitted — awaiting review") | PASS |
| 4 | `techlead@demo` opens the mission's Submissions table → review page shows the evidence links | PASS |
| 5 | **Request changes** with written feedback sets NEEDS_REVISION | PASS |
| 6 | Applicant receives the WARNING notification ("Revision requested: …") | PASS |
| 7 | Reviewer feedback is displayed on the applicant's mission page | PASS |
| 8 | Applicant edits evidence and **Resubmit for review** succeeds (the SEM loop) | PASS |
| 9 | Tech lead **Accept submission** → ACCEPTED; review buttons disappear (terminal) | PASS |
| 10 | Applicant receives the SUCCESS notification and sees locked, accepted evidence | PASS |
| 11 | `hr@demo` opens the review page read-only — no decision buttons | PASS |

Total: 11 assertions — 11 PASS, 0 FAIL.

## Defects Found During Testing

- None in the feature under test. (One environmental note: the two `ops` regression scenarios fail
  when the local Ops Console host process is not running; starting it via `npm run ops:start`
  restores 0 failures. Not related to this baseline.)

## Regression Impact

- Unit suite: 161 → **187 tests**, all passing.
- Scenario suite: 15 → **18 scenarios** (submission loop, review-capability matrix, cross-tenant
  submission isolation); 1 pre-existing documented skip remains.
- Schema change: `20260706090000_v0_15_0_mission_submissions` (see Data Model / Data Dictionary).
