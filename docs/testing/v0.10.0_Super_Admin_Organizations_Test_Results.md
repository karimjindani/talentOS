# v0.10.0 — Super Admin Organizations Console — Test Results

Code version: `v0.10.0`

Date: `2026-07-01`

## Automated Tests

### Unit / Regression Suite

Command: `npm run test`

Result: **46 tests passed** (45 before + 1 new slug-validation test)

```
 ✓ packages/auth/src/operations.test.ts        (3 tests)
 ✓ packages/storage/src/keys.test.ts           (5 tests)
 ✓ packages/auth/src/workflow.test.ts          (9 tests)
 ✓ apps/ops/src/commands.test.ts               (1 test)
 ✓ packages/auth-web/src/roles.test.ts         (6 tests)
 ✓ packages/db/src/regression.test.ts          (2 tests)
 ✓ apps/ops/src/security.test.ts               (2 tests)
 ✓ apps/ops/src/jobs.test.ts                   (1 test)
 ✓ apps/ops/src/health.test.ts                 (2 tests)
 ✓ packages/auth/src/auth.test.ts              (15 tests)

Test Files: 10 passed
Tests:      46 passed
```

New test: `describe("tenant slug validation")` → `"accepts dns-safe slugs and rejects unsafe ones"` in
`packages/auth/src/auth.test.ts`. Capability coverage (`createOrganization` → SUPER_ADMIN true /
ORG_ADMIN false) already existed in the capability-matrix tests.

### Type Check

Command: `npm run typecheck` → **Clean** (exit 0)

### Lint

Command: `npm run lint` → **Clean** (exit 0, 0 warnings)

## Manual Test Plan (after `docker compose up -d --build admin`)

No migration required (no schema change).

| # | Scenario | Steps | Expected |
|---|---|---|---|
| 1 | Super admin sees console | Login `superadmin@talentos.local` / `ChangeMeSuper#1` at `localhost:3200` (complete first-login password/TOTP if prompted) | Sidebar shows **Organizations**; `/organizations` lists existing `demo` tenant |
| 2 | Create tenant | `/organizations` → name "Acme Corp", slug "acme", primary `#db2777`, admin email `orgadmin@acme.talentos.local` → Create | New row appears; `tenants` has `acme`; `tenant_memberships` has an ORG_ADMIN row; `audit_logs` has `organization.created` |
| 3 | Tenant is host-resolvable | Visit `http://acme.localhost:3200` | Admin shell resolves the **acme** tenant (name + branding); applicant `http://acme.localhost:3100` applies acme colors |
| 4 | Slug validation | Submit slug `Acme!` | Rejected: "Slug must be lowercase letters, numbers and hyphens…" |
| 5 | Color validation | Submit primary `red` | Rejected: "Invalid primary color…" |
| 6 | Duplicate slug | Create with slug `demo` | Friendly error: `A tenant with slug "demo" already exists.`; nothing partially created (transaction) |
| 7 | RBAC — no link | Login `orgadmin@demo.talentos.local` | No Organizations link in sidebar |
| 8 | RBAC — direct nav blocked | As org admin, GET `/organizations` | Redirected to `/forbidden` |
| 9 | Org-admin access limitation | New `orgadmin@acme…` logs into `acme.localhost:3200` | Full ORG_ADMIN access only after the `ORG_ADMIN` realm role is granted in Keycloak (documented, D-048) |

## Notes

- `*.localhost` subdomains resolve to loopback automatically in modern browsers — no hosts-file edit
  needed for local testing.
- No schema change: `createOrganization` reuses `Tenant`, `User`, `TenantMembership`, `AuditLog`.
