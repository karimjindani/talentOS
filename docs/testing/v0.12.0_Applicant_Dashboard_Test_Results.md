# Test Results: v0.12.0 — Applicant Dashboard

**Version:** `v0.12.0` · **Decision:** D-059 · **Date:** `2026-07-03`

## Summary

| Metric | Value |
| --- | --- |
| Total test files | 24 |
| Total tests | 125 |
| Passed | 125 |
| Failed | 0 |
| Duration | ~18s |
| New tests this iteration | 24 |

## New Test Files

### `packages/db/src/dashboard.test.ts` (14 tests)

Tests all dashboard DB helper functions using mocked Prisma client:

- **listProgramTasks** — verifies query parameters and ordering (weekNumber asc, order asc)
- **listTasksByWeek** — verifies week-specific filtering
- **listVideoResources** — tests both unfiltered and week-filtered queries
- **listCalendarEvents** — tests default (upcoming only) and includePast flag
- **listUserNotifications** — verifies query with unread-first ordering
- **countUnreadNotifications** — verifies count query with readAt: null filter
- **markNotificationRead** — verifies updateMany with correct where clause
- **createNotification** — verifies create with full input data
- **listCompletedTaskIds** — verifies mapping from completion records to task ID array
- **markTaskCompleted** — verifies upsert with correct where/create
- **getApplicantProgramProgress** — tests progress calculation with tasks across 4 weeks, verifies percentage computation and handling of weeks with no tasks

### `apps/applicant/components/ApplicantShell.test.ts` (10 tests)

Tests the applicant dashboard navigation shell:

- **Exact-match route** — `/dashboard` is active only on exactly `/dashboard`, not on sub-paths
- **startsWith-match routes** — My Program, Tasks, Resources, Calendar, Notifications, Profile each active only on their own path
- **Nav items completeness** — exactly 7 items, all expected routes present

## Regression Verification

All 101 pre-existing tests from v0.11.4 continue to pass unchanged. The 24 new tests bring the total to 125. No existing tests were modified.

## Test Command

```bash
npx vitest run --reporter=verbose
```

## Manual Testing Checklist

- [ ] Applicant with ACCEPTED application sees dashboard at `/dashboard`
- [ ] Applicant without accepted application is redirected to `/application`
- [ ] Landing page redirects to `/dashboard` for accepted applicants
- [ ] PortalHeader shows "Dashboard" link instead of "Apply" for accepted applicants
- [ ] `/application` page redirects to `/dashboard` for accepted applicants
- [ ] Dashboard sidebar shows 7 nav items with active state highlighting
- [ ] Unread notification count badge appears in sidebar
- [ ] 4-week progress bars render correctly on overview and program pages
- [ ] Tasks page shows tasks grouped by week with status badges
- [ ] Resources page embeds YouTube videos and shows external link fallback
- [ ] Calendar page shows upcoming and past events
- [ ] Notifications page allows marking as read
- [ ] Profile page shows applicant info and application details
- [ ] Seed script creates demo data successfully
