# v0.19.5 Weekly Tasks And Submission Readiness - Test Results

**Version**: `v0.19.5`

**Execution date**: 2026-07-19 (Asia/Karachi; regression run IDs use UTC date 2026-07-18)

**Branch**: `engineering-journal-mvp`

**Code evidence commit**: `2b3afce`

**Plan**: `docs/plans/v0.19.5_Weekly_Tasks_And_Submission_Readiness.md`

## Summary

Prisma validation/generation, migration deployment check, 473 unit tests, typecheck, lint, both portal
builds, and every implementation-affected regression area passed. The complete 40-scenario dashboard
run finished 37 passed, 2 failed and 1 skipped. The two failures are the host-only Ops Console login
and session checks because the console was not running on `127.0.0.1:3300`; the storage browser
round-trip is the existing documented skip.

These results do not convert environmental failures or deferred browser checks into passes.

## Gate Commands

| Command | Date | Result | Evidence / notes |
| --- | --- | --- | --- |
| `npx prisma validate --schema packages/db/prisma/schema.prisma` | 2026-07-19 | Pass | Schema valid. |
| `npm run db:generate` | 2026-07-19 | Pass | Prisma Client `v6.19.3` generated. |
| `npx prisma migrate status --schema packages/db/prisma/schema.prisma` | 2026-07-19 | Pass | 18 migrations found; database schema up to date. |
| `npm run db:migrate` | 2026-07-19 | Pass | 18 migrations found; no pending migrations. The iteration migration was already applied locally. |
| `npm test` | 2026-07-19 | Pass | 47 files, 473 tests passed; duration 5.73 s. |
| `npm run typecheck` | 2026-07-19 | Pass | Root, Applicant, Admin and Ops TypeScript checks completed. |
| `npm run lint` | 2026-07-19 | Pass | Applicant and Admin ESLint completed with `--max-warnings=0`. |
| `npm run build` | 2026-07-19 | Pass with warning | Applicant generated 22 pages; Admin generated 16 pages. Existing warning: Next.js ESLint plugin not detected. |
| `npm run regression:applicant` | 2026-07-19 | Pass | 3/3; run `regression-20260718213025-7319bb16`. |
| `npm run regression:admin` | 2026-07-19 | Pass | 4/4; run `regression-20260718213027-c8bfed83`. |
| `npm run regression:programs` | 2026-07-19 | Pass | 2/2; run `regression-20260718213029-68904aa2`. |
| `npm run regression:missions` | 2026-07-19 | Pass | 10/10; run `regression-20260718213009-13608dec`. |
| `npm run regression:tenant` | 2026-07-19 | Pass | 6/6; run `regression-20260718213030-3731fa42`. |
| `npm run regression:dashboard` | 2026-07-19 | Pass | 3/3; run `regression-20260718213032-0e9ca578`. |
| `npm run regression:unit` | 2026-07-19 | Pass | 1/1; run `regression-20260718213055-d3a34100`. |
| `npm run regression:all` | 2026-07-19 | Partial / environment | 37/40 passed, 2 failed, 1 skipped; run `regression-20260718213137-ee21a8c4`. |
| `npm run local:doctor` | 2026-07-19 | Partial / environment | Applicant, Admin, both tenant routes, Keycloak and MinIO passed; Ops Console failed because port 3300 was not running. |

## Full Regression Details

The complete run's two failures were:

| Area | Scenario | Result | Reason |
| --- | --- | --- | --- |
| Ops | Org Admin can complete Ops Console login | Failed | `fetch failed`; host-only Ops process was not running at `127.0.0.1:3300`. |
| Ops | Ops session endpoint returns status envelope | Failed | `fetch failed`; same unavailable host-only process. |

The skipped scenario was:

| Area | Scenario | Result | Reason |
| --- | --- | --- | --- |
| Storage | Storage browser upload/download scenario | Skipped | Existing runner message: full CV upload/download automation is documented as missing for a future storage-focused slice. |

All Auth, Applicant, Admin, Programs, Missions, Journal, Tenant, Dashboard and Unit scenarios passed
inside the full run. The affected area commands were also executed independently and passed as listed
above.

## Scenario Results

These rows match the plan's Test Scenarios section one-for-one.

| Plan scenario | Result | Evidence / limitations |
| --- | --- | --- |
| S1: Weekly tasks and ordered resources are scoped to program week | Pass | Unit suite; Applicant 3/3; Admin 4/4. Safe Markdown tests passed. |
| S2: Weekly task completion is idempotent and tenant scoped | Pass | Dashboard helper unit tests; Applicant and Tenant regressions passed. |
| S3: Journal dates and structured fields are validated | Pass with manual follow-up | Journal unit tests and Applicant future-date scenario passed. Physical-keyboard confidence interaction was not rerun. |
| S4: Readiness counts four journals from only the current attempt | Pass | Readiness unit tests; Missions 10/10; Tenant 6/6. |
| S5: Evidence parsing supports one or more deployment URLs | Pass | URL-safety, readiness and submission tests passed; Missions readiness scenario passed. |
| S6: Unsafe or unreachable evidence is rejected per URL | Pass with deterministic network coverage | Unit and Missions tests passed using controlled network stubs; no new live third-party matrix was run. |
| S7: Failed submission checks do not change durable state | Pass | Submission unit tests and Missions readiness scenario verified no status/lock write. |
| S8: Successful submission locks only current-attempt journals | Pass | Submission tests and Missions selective-lock scenario passed. |
| S9: Revision and repeat attempts remain separated | Pass | Submission tests and Missions repeat/history scenarios passed without duplicate/advance loop. |
| S10: Admin content management retains authorization and tenant scope | Pass | Program-content tests; Programs 2/2; Admin content scenario passed. |
| S11: Applicant and Admin render deployment URLs separately | Partial | Central link-builder and stored multi-URL review data passed; exact browser DOM assertion was not automated or rerun manually in this pass. |
| S12: Existing Regression Run dashboard reports the new coverage | Partial / environment | Runner emitted the existing `REGRESSION_RESULT_JSON` envelope for all areas; Ops web UI was unavailable because the host process was not running. |

## Manual Verification Evidence

No new authenticated browser walkthrough was performed during the 2026-07-19 documentation-only pass.
The existing branch report
`docs/developer-notes/2026-07-16_Weekly_Tasks_Journal_Readiness_Full_Report.md` records a 2026-07-16
manual walkthrough of:

- Applicant Tasks/Resources, three task completions, safe Markdown, pending-video state, journal field
  guidance, four-entry progress and failed-URL no-lock behavior;
- Admin task/resource content, ordering, missing-resource warnings and existing permissions; and
- the Ops dashboard rendering the categorized regression results available at that time.

That older manual record is supporting evidence only. It was not independently repeated here, and its
older test counts must not replace the current command results above.

## Environmental Warnings And Retries

- Initial sandboxed `npm test`, `npm run regression:unit`, `npm run db:migrate` and Prisma migration-
  status attempts could not spawn `esbuild`/Prisma worker executables (`EPERM`). Each required command
  was rerun with process permission and produced the final results above. The startup restriction is
  not counted as a product test failure.
- `npm run build` emitted the existing warning that the Next.js ESLint plugin was not detected in each
  portal's ESLint configuration; both builds completed successfully.
- Prisma reported an available major upgrade (`6.19.3` to `7.8.0`). No dependency upgrade was made.
- The Ops Console is host-run, not a Compose service; it was not started for this docs-only pass.

## Migration Evidence

Migration `20260716090000_weekly_tasks_submission_readiness` exists in the branch. The local database
reported all 18 repository migrations applied and `npm run db:migrate` reported no pending migration.
Prisma Client regeneration remains a required deployment step after pulling this schema.

## Remaining Validation Gaps

- Start `npm run dev:ops`, rerun `npm run local:doctor`, then rerun `npm run regression:all` to close
  the two environment-only Ops failures and visually confirm the current Ops dashboard.
- Add a browser-level assertion that Applicant and Admin pages render every stored deployment URL as a
  separate link with safe external-link attributes.
- Repeat the confidence-rating interaction with a physical keyboard or browser automation that reliably
  dispatches native radio navigation.
- Supply and verify the final introduction YouTube URL.
- The storage browser upload/download scenario remains intentionally skipped and unrelated to this slice.
