# v0.1.0 Test Results

Code version: `v0.1.0`

Baseline commit: `4e2390ce270ef1e049652495885d792a0cbed959`

Test date: `2026-06-27`

## Summary

The `v0.1.0` Initial Platform Architecture Scaffold passed the required validation checks.

## Test Commands and Results

| Command | Result | Notes |
| --- | --- | --- |
| `npm.cmd run db:generate` | Passed | Prisma Client generated successfully. |
| `npm.cmd run test` | Passed | 1 test file passed, 9 tests passed. |
| `npm.cmd run typecheck` | Passed | TypeScript completed with no errors. |
| `npm.cmd run build` | Passed | Next.js production build completed successfully. Non-failing warning: Next.js plugin not detected in ESLint configuration. |
| `npm.cmd run lint` | Passed | ESLint completed with zero warnings. |
| `docker compose config --quiet` | Passed | Docker Compose configuration is valid. |
| `git diff --check` | Passed | No whitespace errors detected. |

## Regression Coverage

The initial regression suite covers:

- tenant resolution,
- role authorization,
- tenant isolation rejection,
- password hashing and verification,
- TOTP enrollment and verification,
- application status transitions.

## Security Notes

The baseline includes security utility coverage for password hashing, TOTP verification, tenant isolation, and role checks.

`npm audit` reported 2 moderate dependency findings during implementation. `npm audit fix --force` was not applied because it can introduce breaking dependency changes and should be reviewed separately.

Prisma also reported that a major version update is available. The baseline keeps the current installed Prisma version to avoid introducing a major-version migration into `v0.1.0`.

## Conclusion

`v0.1.0` is accepted as the first TalentOS code baseline.
