# v0.2.1 Applicant Admin-Link Removal Test Results

Code version: `v0.2.1`

Previous baseline: `v0.2.0`

Date: `2026-06-27`

## Scope

Confirms the applicant application no longer exposes any administrator navigation, while keeping all
previously committed work and route-level isolation functional.

## Static Checks

| Command | Result |
| --- | --- |
| `npm run typecheck` | Passed (packages + applicant + admin) |
| `npm run lint -w @talentos/applicant` | Passed (`--max-warnings=0`) |
| `npm run build -w @talentos/applicant` | Passed (standalone output) |

## Runtime Verification

Applicant container rebuilt and restarted on `http://localhost:3100`.

| Check | Result |
| --- | --- |
| Page contains an `Admin` label | No |
| Page contains the admin host (`localhost:3200`) | No |
| Applicant header navigation links | `Apply`, `Login` only |
| Admin route on applicant container (`/applications`) | 404 (isolation intact) |

## Regression

The `@talentos/auth` unit/regression suite (9 tests) is unaffected by this UI-only change and continues
to pass. Route-level module isolation from `v0.2.0` is unchanged.

## Conclusion

The applicant portal exposes no administrator route or button. Module isolation is complete at both the
routing and navigation levels. `v0.2.1` is validated.
