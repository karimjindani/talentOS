# v0.7.1 Applicant Self-Signup Test Results

Code version: `v0.7.1`

Previous baseline: `v0.7.0` (`7d73552`)

Date: `2026-06-29`

## Scope

Verifies applicant self-signup via Keycloak self-registration and the applicant-portal "Create account"
entry point.

## Static Checks

| Check | Result | Notes |
| --- | --- | --- |
| `npm run typecheck` (tsc ×3) | Passed | No errors. |
| `npm run lint` (both apps) | Passed | `--max-warnings=0`. |
| `npm run test` (Vitest) | Passed | 33 tests unchanged (config + UI only). |
| Realm re-import | Passed | Recreated the Keycloak DB volume; realm imported with registration enabled. |
| `docker compose up --build admin applicant` | Passed | Applicant rebuilt with the signup UI. |

## Functional Checks

| Check | Result | Notes |
| --- | --- | --- |
| Realm `registrationAllowed` is true | Passed | OIDC registration endpoint returns the hosted register form. |
| "Create account" entry on `/login` | Passed | Starts Keycloak registration via `prompt=create`. |
| New registrant defaults to APPLICANT | Passed | `defaultRoles: ["APPLICANT"]`; username = email. |
| New applicant can sign in and reach `/apply` | Passed | Apply flow provisions/links the DB user on submit. |
| Existing demo users still sign in | Passed | Seeded `applicant@`, `orgadmin@`, `hr@`, `techlead@`, `superadmin@` unaffected. |

## Notes

- Keycloak owns the signup form, password policy and TOTP; the applicant portal only links into it.
- Applying the realm change to an existing environment requires re-importing the realm (recreate the
  Keycloak DB volume) or toggling the setting in the Keycloak admin console.
