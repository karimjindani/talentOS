# AI Mentor Unit Test Cases — v0.15.0

**Code version:** `v0.15.0`  
**Branch:** `feat/applicant-ai-mentor-skeleton`  
**Date:** 2026-07-09  

---

## Overview

Unit tests use **Vitest** and run via `npm.cmd run regression:unit` or `npx vitest run`.  
All tests are fast, isolated, and do not require Docker or network access (mock external dependencies).

---

## 1. Chat UI Unit Tests

### UT-UI-01: Conversation State Initialization

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | Page initializes with empty conversations and loading state |
| **Test** | `it("initializes with isLoadingHistory=true and empty conversations")` |
| **Assertions** | `isLoadingHistory` is `true` on mount; `conversations` is `[]`; `activeConversationId` is `null` |

### UT-UI-02: Welcome Message Rendering

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | Welcome message renders with correct content |
| **Test** | `it("renders welcome message with markdown content")` |
| **Assertions** | Welcome message contains "Welcome to AI Mentor!"; has `role: "mentor"`; markdown is rendered as HTML |

### UT-UI-03: New Conversation Creation

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | `createNewConversation()` produces a valid conversation object |
| **Test** | `it("creates a conversation with unique id, welcome message, and correct timestamps")` |
| **Assertions** | `id` starts with `conv-`; `title` is `"New Chat"`; `messages` has 1 welcome message; `createdAt === updatedAt` |

### UT-UI-04: Title Derivation from First User Message

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | `deriveTitle()` extracts a short title from the first user message |
| **Test** | `it("derives title from first user message, truncated to 40 chars")` |
| **Assertions** | Short message → exact text; Long message → 40 chars + "…"; No user message → `"New Chat"`; Markdown chars stripped |

### UT-UI-05: Per-Conversation Loading State Isolation

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | Loading state is per-conversation, not global |
| **Test** | `it("only the active conversation shows isLoading when sending")` |
| **Assertions** | When sending in conv A, conv B's `isLoading` is `false`; switching to conv B shows no loading indicator |

### UT-UI-06: Suggested Questions

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | 9 suggested question chips are rendered |
| **Test** | `it("renders 9 suggested question chips")` |
| **Assertions** | All 9 questions present: Today's Task, Show Progress, Timeline, My Missions, Explain SDLC, Explain SEM, Testing Strategy, Deployment, PRD Guide |

### UT-UI-07: Elapsed Timer for "Still Working" UX

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | Elapsed timer starts when `isSending` is true, resets when false |
| **Test** | `it("tracks elapsed seconds while waiting for AI response")` |
| **Assertions** | `elapsedSeconds` increments every 1s while `isSending`; resets to 0 when `isSending` becomes false |

---

## 2. API Route Unit Tests

### UT-API-01: GET — Unauthenticated Request

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/api/ai/mentor/route.ts` |
| **Description** | GET without auth returns 401 |
| **Test** | `it("returns 401 for unauthenticated GET")` |
| **Assertions** | Status 401; body `{ error: "Unauthorized" }` |

### UT-API-02: GET — Authenticated Request Returns History

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/api/ai/mentor/route.ts` |
| **Description** | GET with auth returns conversation history |
| **Test** | `it("returns conversation history for authenticated user")` |
| **Assertions** | Status 200; body has `messages` array; messages have `id`, `role`, `content`, `timestamp` |

### UT-API-03: POST — Empty Prompt Rejected

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/api/ai/mentor/route.ts` |
| **Description** | POST with empty prompt returns 400 |
| **Test** | `it("returns 400 for empty prompt")` |
| **Assertions** | Status 400; body `{ error: "Prompt is required" }` |

### UT-API-04: POST — Over-Length Prompt Rejected

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/api/ai/mentor/route.ts` |
| **Description** | POST with prompt > 2000 chars returns 400 |
| **Test** | `it("returns 400 for prompt exceeding 2000 characters")` |
| **Assertions** | Status 400; body contains `"at most 2000 characters"` |

### UT-API-05: POST — Invalid JSON Body

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/api/ai/mentor/route.ts` |
| **Description** | POST with invalid JSON returns 400 |
| **Test** | `it("returns 400 for invalid JSON body")` |
| **Assertions** | Status 400; body `{ error: "Invalid JSON body" }` |

### UT-API-06: POST — Unauthenticated Request

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/api/ai/mentor/route.ts` |
| **Description** | POST without auth returns 401 |
| **Test** | `it("returns 401 for unauthenticated POST")` |
| **Assertions** | Status 401; body `{ error: "Unauthorized" }` |

### UT-API-07: POST — Successful Response

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/api/ai/mentor/route.ts` |
| **Description** | POST with valid prompt returns mentor response |
| **Test** | `it("returns AI mentor response for valid prompt")` |
| **Assertions** | Status 200; body has `message` string; body has `status` field; message persisted to DB |

---

## 3. Context Builder Unit Tests

### UT-CTX-01: Build Context with Accepted Application

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai-context.ts` |
| **Description** | `buildApplicantContext()` returns full context for accepted applicant |
| **Test** | `it("builds context with program, progress, tasks, missions")` |
| **Assertions** | `context.program` is defined; `context.progress` has `overallPercentage`; `context.upcomingTasks` is an array; `context.missions` is an array |

### UT-CTX-02: Build Context with No Application

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai-context.ts` |
| **Description** | `buildApplicantContext()` returns safe fallback for no application |
| **Test** | `it("returns safe fallback context when no accepted application")` |
| **Assertions** | `context.program` is undefined or null; no crash; all fields have safe defaults |

### UT-CTX-03: Context to Prompt Section

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai-context.ts` |
| **Description** | `contextToPromptSection()` produces formatted string |
| **Test** | `it("formats context as markdown section")` |
| **Assertions** | Output contains program name; output contains progress percentage; output is a string |

### UT-CTX-04: Tenant Isolation in Context

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai-context.ts` |
| **Description** | Context queries are scoped by tenantId + userId |
| **Test** | `it("only retrieves data for the given tenant and user")` |
| **Assertions** | Cross-tenant data is not included; queries use tenantId filter |

---

## 4. Knowledge Base Unit Tests

### UT-KB-01: Keyword Retrieval

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/knowledge-base.ts` |
| **Description** | `retrieveKnowledge()` returns matching snippets for keywords |
| **Test** | `it("returns knowledge snippets matching prompt keywords")` |
| **Assertions** | Query "SDLC" returns SDLC snippet; snippet has `title`, `source`, `content`, `score` |

### UT-KB-02: No Match Returns Empty Array

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/knowledge-base.ts` |
| **Description** | `retrieveKnowledge()` returns empty array for no matches |
| **Test** | `it("returns empty array when no keywords match")` |
| **Assertions** | Query "xyz random" returns `[]` |

### UT-KB-03: Limit Parameter

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/knowledge-base.ts` |
| **Description** | `retrieveKnowledge(prompt, 2)` returns at most 2 snippets |
| **Test** | `it("respects the limit parameter")` |
| **Assertions** | Result length ≤ 2 |

### UT-KB-04: Score Ranking

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/knowledge-base.ts` |
| **Description** | Snippets are ranked by keyword match score |
| **Test** | `it("sorts results by score descending")` |
| **Assertions** | `results[0].score >= results[1].score` |

### UT-KB-05: Knowledge to Prompt Section

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/knowledge-base.ts` |
| **Description** | `knowledgeToPromptSection()` formats snippets for system prompt |
| **Test** | `it("formats knowledge snippets as prompt section")` |
| **Assertions** | Output contains snippet titles; output is a string; empty input returns empty string |

---

## 5. RBSE Unit Tests

### UT-RBSE-01: Block Inappropriate Questions

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai-rbse.ts` |
| **Description** | `classifyQuestion()` blocks off-topic or harmful questions |
| **Test** | `it("blocks questions about non-internship topics")` |
| **Assertions** | Action type is `"blocked"`; response has refusal message; no LLM call needed |

### UT-RBSE-02: Direct Answer for Known Patterns

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai-rbse.ts` |
| **Description** | `classifyQuestion()` provides direct answers for common queries |
| **Test** | `it("returns direct answer for 'what is SDLC'")` |
| **Assertions** | Action type is `"direct_answer"`; response contains SDLC explanation |

### UT-RBSE-03: Allow LLM for Complex Questions

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai-rbse.ts` |
| **Description** | `classifyQuestion()` allows LLM for complex/novel questions |
| **Test** | `it("allows LLM for complex internship questions")` |
| **Assertions** | Action type is `"allow_llm"` |

### UT-RBSE-04: Allowed Topics List

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai-rbse.ts` |
| **Description** | Allowed topics include internship, task, mission, SDLC, SEM, etc. |
| **Test** | `it("recognizes all allowed topic keywords")` |
| **Assertions** | All keywords in `ALLOWED_TOPICS` are recognized |

---

## 6. GLM Integration Unit Tests

### UT-LLM-01: Has LLM Config

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | `hasLLMConfig()` returns true when API key is set |
| **Test** | `it("returns true when GLM_Z_API_KEY is set")` |
| **Assertions** | `hasLLMConfig()` is `true` when key present; `false` when empty/undefined |

### UT-LLM-02: System Prompt Construction

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | `buildSystemPrompt()` produces a concise system prompt |
| **Test** | `it("builds system prompt under 1500 chars with context and knowledge")` |
| **Assertions** | Prompt contains role description; prompt contains rules; prompt length < 1500; includes context section if provided; includes knowledge section if provided |

### UT-LLM-03: Token Limit Configuration

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | `LLM_MAX_TOKENS` defaults to 1024 |
| **Test** | `it("uses 1024 max tokens by default")` |
| **Assertions** | `LLM_MAX_TOKENS === 1024` when env not set; respects env override |

### UT-LLM-04: Timeout Configuration

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | `LLM_TIMEOUT_MS` is 60000ms |
| **Test** | `it("uses 60 second timeout")` |
| **Assertions** | `LLM_TIMEOUT_MS === 60_000` |

### UT-LLM-05: Request Body Format

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | `callGLM()` sends OpenAI-compatible request body |
| **Test** | `it("sends correct model, messages, max_tokens, temperature, stream:false")` |
| **Assertions** | Body has `model`, `messages` (system+user), `max_tokens`, `temperature`, `stream: false` |

### UT-LLM-06: Auth Error Handling

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | 401/403 responses throw `LLM_AUTH_ERROR` without retry |
| **Test** | `it("throws LLM_AUTH_ERROR on 401/403 without retrying")` |
| **Assertions** | Error message is `"LLM_AUTH_ERROR"`; no retry attempted |

### UT-LLM-07: Rate Limit Handling

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | 429 response throws `LLM_RATE_LIMIT` |
| **Test** | `it("throws LLM_RATE_LIMIT on 429")` |
| **Assertions** | Error message is `"LLM_RATE_LIMIT"` |

### UT-LLM-08: Timeout Handling

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | AbortController fires after 60s, throws `LLM_TIMEOUT` without retry |
| **Test** | `it("throws LLM_TIMEOUT on abort without retrying")` |
| **Assertions** | Error message is `"LLM_TIMEOUT"`; no retry on timeout |

### UT-LLM-09: Retry on Network Error

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | Network/5xx errors trigger 1 retry |
| **Test** | `it("retries once on 500/network error")` |
| **Assertions** | 2 total attempts; delay between attempts |

### UT-LLM-10: Response Parsing

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | Successfully parses GLM chat completion response |
| **Test** | `it("extracts content from choices[0].message.content")` |
| **Assertions** | Returns `{ content, usage }`; content is a non-empty string |

---

## 7. Error Handling & Fallback Unit Tests

### UT-ERR-01: Fallback to Stub on LLM Failure

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | `requestAIInteraction()` falls back to stub when LLM fails |
| **Test** | `it("returns stub response with error status when LLM fails")` |
| **Assertions** | `status` is `"error"`; message contains stub content + degradation note |

### UT-ERR-02: Stub Response When No API Key

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | `requestAIInteraction()` uses stub when no API key configured |
| **Test** | `it("returns stubbed response when no LLM API key")` |
| **Assertions** | `status` is `"stubbed"`; no LLM call attempted |

### UT-ERR-03: Stub Response with Knowledge

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | Stub response includes knowledge snippets when available |
| **Test** | `it("returns knowledge content in stub response")` |
| **Assertions** | Message contains knowledge content; cards include tips with additional sources |

### UT-ERR-04: Stub Response with Context

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | Stub response uses applicant context for personalized responses |
| **Test** | `it("uses context data in stub responses")` |
| **Assertions** | Task query returns actual task from context; progress query returns actual percentage |

### UT-ERR-05: Detailed Request Gets More Tokens

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/lib/ai.ts` |
| **Description** | Prompts with "detailed"/"complete" get up to 2048 tokens |
| **Test** | `it("allocates 2048 tokens for detailed requests")` |
| **Assertions** | `maxTokens >= 2048` when prompt contains "detailed", "complete", "in detail", "all phases", "explain everything" |

---

## 8. Loading State Unit Tests

### UT-LOAD-01: Loading State Set on Send

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | `isLoading` is set to true on the target conversation when sending |
| **Test** | `it("sets isLoading=true on target conversation during send")` |
| **Assertions** | Target conversation `isLoading === true`; other conversations unaffected |

### UT-LOAD-02: Loading State Cleared on Success

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | `isLoading` is cleared when response received |
| **Test** | `it("clears isLoading after successful response")` |
| **Assertions** | `isLoading === false` after response; mentor message appended |

### UT-LOAD-03: Loading State Cleared on Error

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | `isLoading` is cleared on error |
| **Test** | `it("clears isLoading on error")` |
| **Assertions** | `isLoading === false` after error; error state set |

### UT-LOAD-04: Loading State Not Persisted

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | `isLoading` is stripped before saving to localStorage |
| **Test** | `it("does not persist isLoading to localStorage")` |
| **Assertions** | `saveConversationsToStorage` strips `isLoading`; `loadConversationsFromStorage` sets `isLoading: false` |

---

## 9. Conversation Persistence Unit Tests

### UT-PERSIST-01: Save to localStorage

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | `saveConversationsToStorage()` persists conversations |
| **Test** | `it("saves conversations to localStorage with key talentos:mentor-conversations")` |
| **Assertions** | `localStorage.getItem("talentos:mentor-conversations")` returns JSON; isLoading stripped |

### UT-PERSIST-02: Load from localStorage

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | `loadConversationsFromStorage()` loads and revives Date objects |
| **Test** | `it("loads conversations and revives Date objects")` |
| **Assertions** | Messages have `timestamp` as `Date` instances; `isLoading` is `false` |

### UT-PERSIST-03: Corrupted localStorage Data

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | Corrupted JSON in localStorage returns empty array |
| **Test** | `it("returns empty array for corrupted localStorage data")` |
| **Assertions** | `loadConversationsFromStorage()` returns `[]` on JSON parse error |

### UT-PERSIST-04: Storage Unavailable

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/page.tsx` |
| **Description** | `saveConversationsToStorage()` silently ignores when storage unavailable |
| **Test** | `it("silently handles localStorage quota exceeded")` |
| **Assertions** | No crash; no uncaught exception |

---

## 10. Component Unit Tests

### UT-COMP-01: MessageBubble — User Message

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/components/MessageBubble.tsx` |
| **Description** | User messages render right-aligned with correct styling |
| **Test** | `it("renders user message right-aligned")` |
| **Assertions** | Container has right alignment; content is plain text |

### UT-COMP-02: MessageBubble — Mentor Message with Markdown

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/components/MessageBubble.tsx` |
| **Description** | Mentor messages render markdown with syntax highlighting |
| **Test** | `it("renders markdown content with code blocks")` |
| **Assertions** | Markdown is parsed; code blocks have syntax highlighting; GFM tables supported |

### UT-COMP-03: CardRenderer — Task Card

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/components/CardRenderer.tsx` |
| **Description** | Task card renders title, description, due date, estimated time |
| **Test** | `it("renders task card with all fields")` |
| **Assertions** | Title, description, dueDate, estimatedTime all rendered |

### UT-COMP-04: CardRenderer — Progress Card

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/components/CardRenderer.tsx` |
| **Description** | Progress card renders percentage bar |
| **Test** | `it("renders progress card with percentage")` |
| **Assertions** | Percentage displayed; progress bar width matches percentage |

### UT-COMP-05: CardRenderer — Timeline Card

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/components/CardRenderer.tsx` |
| **Description** | Timeline card renders list of items |
| **Test** | `it("renders timeline card with items list")` |
| **Assertions** | All items rendered in order |

### UT-COMP-06: CardRenderer — Tips Card

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/components/CardRenderer.tsx` |
| **Description** | Tips card renders list of tips |
| **Test** | `it("renders tips card with items list")` |
| **Assertions** | All tips rendered with bullet styling |

### UT-COMP-07: CardRenderer — Badge Card

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/components/CardRenderer.tsx` |
| **Description** | Badge card renders label and value |
| **Test** | `it("renders badge card with label and value")` |
| **Assertions** | Label and value both rendered |

### UT-COMP-08: CardRenderer — Warning Card

| Field | Value |
| --- | --- |
| **Module** | `apps/applicant/app/dashboard/mentor/components/CardRenderer.tsx` |
| **Description** | WarningCard renders warning message |
| **Test** | `it("renders warning card with alert styling")` |
| **Assertions** | Warning icon present; message text rendered |

---

## Summary

| Category | Test Count |
| --- | --- |
| Chat UI | 7 |
| API Route | 7 |
| Context Builder | 4 |
| Knowledge Base | 5 |
| RBSE | 4 |
| GLM Integration | 10 |
| Error Handling & Fallback | 5 |
| Loading States | 4 |
| Conversation Persistence | 4 |
| Components | 8 |
| **Total** | **58** |
