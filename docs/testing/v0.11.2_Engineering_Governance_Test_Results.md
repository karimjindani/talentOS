# v0.11.2 — Engineering Governance Documentation — Test Results

Code version: `v0.11.2` · Date: `2026-07-03`

This is a **documentation-only** baseline (source-control + CI/CD governance policies; D-055, D-056). No
application code, pipeline, or schema change — `.github/workflows/ci.yml` is unchanged. Verification
confirms the existing regression suite still passes and the documented conventions match reality.

## Automated checks

| Command | Result |
|---|---|
| `npm run typecheck` | Passed (root + applicant + admin + ops, exit 0) |
| `npm run lint` | Passed (both apps, `--max-warnings=0`) |
| `npm run test` | **78 tests passed** (19 files) — unchanged from `v0.11.1` |

Suite unchanged because no code changed; the docs baseline must keep the existing suite green
(regression rule).

## Policy-vs-reality cross-check

The documented conventions were transcribed from the actual repository state, not an ideal:

| Documented in policy | Verified against |
|---|---|
| Branch naming `<type>/vX.Y.Z-<slug>` | `git branch -a` — e.g. `feat/v0.11.0-org-admin-provisioning`, `fix/v0.10.3-tenant-isolation`, `ci/github-actions-pipeline` |
| Conventional Commits + `(vX.Y.Z, D-0NN)` trailer | `git log --oneline` — e.g. `feat(auth): reserve infra/routing tenant slugs (v0.11.1, D-054)` |
| PR-merge into `main`, no direct pushes | history shows `Merge pull request #NN from …` merge commits |
| CI gate `db:generate → typecheck → lint → test → build` | `.github/workflows/ci.yml` (push + pull_request, Node 24) |

## Documentation checks

- New policies `docs/Source_Control_Policy.md` and `docs/CI_CD_Pipeline.md` created and linked from
  `docs/sdlc.md` (two new sections), `docs/Deployment.md` (Delivery Pipeline section), and
  `docs/README.md` (Key Documents).
- Enforcement artifacts present: `CONTRIBUTING.md`, `.github/pull_request_template.md`,
  `.github/CODEOWNERS`.
- Doc-control updated: `docs/Version_Baseline.md` (`v0.11.2` baseline), `docs/Decision_Log.md`
  (`D-055`, `D-056`); version banners bumped to `v0.11.2`.
- Internal doc links resolve (sdlc → both policies; README → both policies; Deployment → CI_CD_Pipeline;
  policies → CODEOWNERS / PR template / ci.yml).

## Follow-ups (documented, out of scope here)

- Enable GitHub branch-protection on `main` (PR + CI + ≥1 review, no force-push) — checklist in
  `docs/Source_Control_Policy.md`.
- Implement the security-scan stage and the CD build/push/deploy workflow — design in
  `docs/CI_CD_Pipeline.md` (deferred to a later baseline).

## Notes

- No schema change; Data Model / Data Dictionary unaffected. Regression suite: 78 tests.
