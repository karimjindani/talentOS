# v0.11.4 — UI Polish — Test Results

Code version: `v0.11.4` · Date: `2026-07-03`

## Automated checks

| Command | Result |
|---|---|
| `npm run typecheck` | Passed (root + applicant + admin + ops) |
| `npm run lint` | Passed (both apps, `--max-warnings=0`) |
| `npm run test` | **101 tests passed** (22 test files) |

### New regression tests

**`apps/admin/components/SidebarNav.test.ts`** (12 tests) — covers the `isActive` route-matching logic
that powers the admin sidebar active-state indicator:

| Test group | Cases | What it asserts |
|---|---|---|
| Exact-match routes (`/`) | 2 | Overview is active only on `/`, not on any sub-path |
| startsWith-match routes | 5 | Applications active on `/applications` and `/applications/[id]`; same for Programs, Operations, Settings, Organizations |
| Cross-route isolation | 3 | No false positives — Applications not active on `/programs`, etc.; no item active on unknown routes |
| NAV_ITEMS integrity | 2 | Five standard items in order; only Overview uses exact matching |

### Existing regression suite (unchanged, 78 tests carried forward)

All previously committed tests remain green: auth/RBAC, tenant isolation, application lifecycle,
programs management, object storage, Keycloak OTP/SSO-logout, org-admin provisioning, reserved slugs,
duplicate-application guard.

## Docker rebuild

| Container | Result |
|---|---|
| `talentos-applicant` | Rebuilt and started (`docker compose up -d --build applicant`) |
| `talentos-admin` | Rebuilt and started (`docker compose up -d --build admin`) |

## Manual browser verification

1. **Applicant Apply page** (`http://localhost:3100/apply`):
   - Branded header banner with 📝 icon in blue circle ✓
   - Sectioned form: "Program & Motivation", "Documents", "Profile Links (optional)" ✓
   - Styled inputs with focus rings ✓
   - Dashed-border CV upload zone ✓
   - Full-width submit button with hover state ✓

2. **Admin Portal sidebar** (`http://localhost:3200`):
   - Active nav link shows `bg-brand-blue text-white font-semibold` ✓
   - Inactive links show hover state on mouseover ✓
   - Navigating to `/applications/123` highlights "Applications" in sidebar ✓
   - "Organizations" link visible only for SUPER_ADMIN ✓

3. **Admin Application Review page** (`http://localhost:3200/applications/[id]`):
   - "← Back to Applications" link visible at top of page ✓
   - Clicking it navigates to `/applications` ✓

## Notes

- UI-only iteration — no schema, data-model, or server-action logic change.
- Regression suite grew from 78 to 101 tests (12 new SidebarNav tests + 11 from other existing tests
  not previously counted in the v0.11.1 baseline).
- No new migrations, no new dependencies.
