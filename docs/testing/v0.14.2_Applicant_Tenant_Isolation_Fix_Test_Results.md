# v0.14.2 — Applicant Portal Tenant Isolation Fix — Test Results

Code version: `v0.14.2` · Date: `2026-07-05`

## Automated checks

| Command | Result |
|---|---|
| `npm run build` (applicant image) | Passed — `docker compose up -d --build applicant` (typechecks + compiles, exit 0) |
| `npm run test` | **152 tests passed** (146 prior − 1 stale + 1 fixed + 6 new `tenant-guard.test.ts`) |

New file `apps/applicant/lib/tenant-guard.test.ts` (6 tests) mocks the session (`@/auth`), the
Host→slug resolver (`@talentos/ui`) and the DB (`@talentos/db`) while running the real RBAC helpers, and
asserts `resolveTenantAccess`:

- unauthenticated → `unauthenticated`
- unknown Host-resolved tenant → `unknown-tenant`
- **signed-in non-member of the resolved tenant → `forbidden`** (the ported D-051 gap), verifying the
  membership check keys on the Host-resolved tenant id
- member whose roles do not grant the capability → `forbidden`
- `APPLICANT` member of the resolved tenant → `ok`
- `SUPER_ADMIN` with no membership → `ok` (bypass)

Also fixed a pre-existing failing test: `apps/admin/lib/keycloak-admin.test.ts` still expected the
removed `CONFIGURE_TOTP` required action; it now asserts `UPDATE_PASSWORD` is present and
`CONFIGURE_TOTP` is absent, and additionally pins `registrationAllowed` / `registrationEmailAsUsername`
in the realm import.

## End-to-end verification (real browser — Playwright/Edge, live stack)

Before/after on the running stack (`applicant`, `admin`, `keycloak`, `postgres`, `minio`), rebuilding the
applicant container between the two:

| Scenario | Before | After |
|---|---|---|
| `applicant@demo` → `sbits.lvh.me:3100/application` | sbits ("System bits") portal | **`/access-denied`** ✅ |
| `applicant@demo` → `sbits.lvh.me:3100/dashboard` | sbits portal (→ application) | **`/access-denied`** ✅ |
| `accepted@demo` → `sbits.lvh.me:3100/{dashboard,application}` | — | **`/access-denied`** ✅ |
| `accepted@demo` → demo `/dashboard` | dashboard shell | dashboard shell (unbroken) ✅ |
| `applicant@demo` → demo `/dashboard` | (admits) | admits → `/application` (accepted-only rule) ✅ |
| `applicant@demo` → demo `/apply` | apply form | redirect to `/application` (existing member) ✅ |
| non-member → `sbits.lvh.me:3100/apply` | apply form | **apply form still open** (public funnel) ✅ |
| new user registers under `sbits.lvh.me:3100` → applies | reg disabled (dead login form) | **registers, applies, DB shows sbits `APPLICANT` membership + `SUBMITTED` application** ✅ |

The registration test required first enabling `registrationAllowed` on the drifted live realm and seeding
a published program for `sbits`. Merely registering/landing on a tenant created **no** membership — only
submitting `/apply` did — confirming the isolation model (authenticated ≠ enrolled).

## Deployment

`docker compose up -d --build applicant` rebuilt and started cleanly (exit 0); the applicant portal
serves `200` on the tenant landing page and `307` → `/login` on protected routes. No migration (code-only
change).

## Notes

- No schema, migration, or data-model change. Regression suite: **152 tests**.
- The Keycloak realm role now gates only applicant-portal entry (middleware); per-tenant presence is
  authorized by `TenantMembership`, matching the admin portal (D-051).
- Live-realm drift (registration disabled; `accepted@demo` / `orgadmin@sbp` missing) was patched out of
  band via the `talentos-provisioner` admin API; the committed realm import is already correct, so a fresh
  realm is unaffected.
