# v0.10.2 — Keycloak SSO Logout Fix — Test Results

Code version: `v0.10.2`

Date: `2026-07-01`

## Automated Tests

Command: `npm run test`

Result: **50 tests passed** (47 before + 3 new `buildEndSessionUrl` tests)

New file `packages/auth-web/src/logout.test.ts`:
- uses `id_token_hint` when an id token is present (and omits `client_id`);
- falls back to `client_id` when no id token is available;
- normalizes a trailing slash on the issuer.

`npm run typecheck` → clean. `npm run lint` → clean.

## Keycloak endpoint verification (live)

After rebuilding the portals and live-patching both clients' `post.logout.redirect.uris` via
`kcadm.sh`, the `end_session_endpoint` was probed directly:

| Request | Result |
|---|---|
| `logout?client_id=talentos-admin&post_logout_redirect_uri=http://localhost:3200/` | **HTTP 302** (accepted) |
| `logout?client_id=talentos-applicant&post_logout_redirect_uri=http://localhost:3100/` | **HTTP 302** (accepted) |
| `logout?client_id=talentos-admin&post_logout_redirect_uri=http://evil.example.com/` | **HTTP 400** (rejected — no open redirect) |

Client attribute confirmed persisted (full client GET):
`"post.logout.redirect.uris" : "http://localhost:3200/*"` (admin), `.../3100/*` (applicant).

## Manual verification (the original repro)

| # | Steps | Expected |
|---|---|---|
| 1 | Log in to admin (`localhost:3200`), click **Logout** | Land on the Keycloak login page |
| 2 | Refresh / navigate back to `/` | Still the login page — no silent re-auth (previously it logged you straight back in) |
| 3 | Repeat on applicant (`localhost:3100`) | Same: logout sticks |
| 4 | After logging out of one portal, open the other | Also requires a fresh login (shared Keycloak SSO session is gone) |

## Notes

- No schema or data-model change.
- The running Keycloak realm was patched non-destructively (no volume recreation); a fresh environment
  gets the corrected `post.logout.redirect.uris` from the realm import automatically.
