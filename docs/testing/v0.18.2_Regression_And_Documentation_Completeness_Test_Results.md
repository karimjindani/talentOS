# v0.18.2 Regression And Documentation Completeness Test Results

Date: 2026-07-09

## Summary

All new `journal` and `missions` regression scenarios pass, the full regression suite is green
(27/28, 1 pre-existing documented skip), and the full local gate is unaffected. Documentation changes
were prose-only and re-verified for internal consistency (version stamps, scenario/test counts).

## Gate Commands

| Check | Result |
| --- | --- |
| `npm run typecheck` | Passed |
| `npm run lint` | Passed |
| `npm test` | Passed — 34 test files, 243 tests (unchanged; `regression.test.ts` gained 2 ordering assertions within its existing test case) |
| `npm run build` | Passed |
| `npm run regression:journal` | Passed — 4/4 |
| `npm run regression:missions` | Passed — 6/6 (includes the 2 new scenarios) |
| `npm run regression:all` | Passed — 27/28, 1 pre-existing documented skip (storage), 0 failed |
| `npm run ops:cleanup-regression` | Passed — cleaned all marked data including `EngineeringJournalEntry` rows, confirming the new cleanup wiring works end-to-end |

## Scenario Results

| Scenario | Automation | Result |
| --- | --- | --- |
| Applicant creates and edits a journal entry against their assigned mission | Added this iteration (`regression:journal`) | Passed |
| Applicant cannot create a journal entry against a published mission not assigned to them | Added this iteration (`regression:journal`) | Passed |
| One journal entry per applicant per calendar date is enforced | Added this iteration (`regression:journal`) | Passed |
| Journal entries lock once the mission's assignment is submitted | Added this iteration (`regression:journal`) | Passed |
| Applicant mission list/detail and submission drafting are limited to assigned missions | Added this iteration (`regression:missions`) | Passed |
| An applicant accepted before Mission Assignment existed has no assigned missions | Added this iteration (`regression:missions`) | Passed — documents current (gap) behavior by design; will fail loudly if the underlying behavior changes without updating this scenario |

## Notes

- The "no automatic backfill" scenario is intentionally an assertion of *current* behavior, not a
  statement that it's correct. See `docs/Regression_Scenarios.md` Known Gaps for the open product
  decision (backfill script vs. lazy on-read assignment vs. accepted limitation).
- Documentation-only changes (Deployment, vision, Product Backlog, user guides, CI/CD pipeline,
  Architecture, README, Testing Strategy, Regression Scenarios) have no automated verification beyond
  manual cross-reading for internal consistency; there is no CI doc-lint step today.

## Verification

See `docs/plans/v0.18.2_Regression_And_Documentation_Completeness.md`.
