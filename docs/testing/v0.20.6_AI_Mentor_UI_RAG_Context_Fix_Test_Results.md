# v0.20.6 AI Mentor UI, RAG, Context Fix, And RBSE Unblock Test Results

Date: 2026-08-21

## Summary

All 12 test scenarios from the plan were verified — 4 automated via unit tests, 8 deferred to manual verification (UI interactions and LLM-dependent behavior). The full gate suite passes: typecheck, lint, 1020 unit tests, and build. The Docker container was rebuilt with `--force-recreate` and the LiteLLM proxy verified reachable. Manual testing confirmed the mentor now correctly reports Mission 1 as "in progress, not submitted" instead of "completed", and "tell me about mission 1" reaches the LLM instead of being blocked.

## Gate Commands

| Check | Result |
| --- | --- |
| `npm run typecheck` | Passed — `tsc --noEmit` for applicant, admin, ops, root — zero errors |
| `npm run lint` | Passed — `eslint . --max-warnings=0` for applicant and admin — zero warnings |
| `npm test` | Passed — 80 test files, 1020 tests, 0 failures |
| `npm run build` | Passed — `npm run build:docs-index` (347 sections indexed) + Next.js build for applicant and admin |
| `npm run regression:all` | Not run — regression suite requires a running Docker stack with seeded data; unit tests + manual verification used instead |

## Scenario Results

| Scenario | Automation | Result |
| --- | --- | --- |
| S1: Mentor answers "tell me about mission 1" with correct status | Deferred — LLM + seeded data | Passed — manual: mentor responded "in progress, not yet submitted, 0 journal entries" |
| S2: "tell me about mission 1" is not blocked by RBSE | Added this iteration (`ai-rbse.test.ts`) | Passed — `classifyQuestion` returns `allow_llm`; server log confirms `RBSE allowing LLM` |
| S3: "tell me about John" is still blocked by RBSE | Added this iteration (`ai-rbse.test.ts`) | Passed — personal-name patterns still block after removing broad `"tell me about "` |
| S4: Mentor does not append "Next step" follow-up questions | Deferred — LLM response inspection | Passed — manual: system prompt rule verified in `buildSystemPrompt`; response ended without "Next step:" |
| S5: Suggested questions send immediately | Deferred — UI interaction | Passed — manual: clicking a chip sends immediately without extra click |
| S6: Card CTAs are functional | Deferred — UI interaction | Passed — manual: "Start Task" navigates, "Save Tips" persists + shows "Saved!" |
| S7: Per-message actions work | Deferred — UI interaction | Passed — manual: copy, regenerate, edit-and-resend, thumbs up/down all functional |
| S8: Mobile drawer opens and closes | Deferred — responsive UI | Passed — manual: hamburger opens drawer, selecting conversation closes it |
| S9: Conversation search, rename, and pin | Deferred — UI interaction | Passed — manual: search filters, double-click renames, pin sorts to top |
| S10: Context shows correct per-mission journal count and task completion | Added this iteration (`ai-context.test.ts`) | Passed — `missionStatus` fields asserted; `contextToPromptSection` renders unambiguous labels |
| S11: RAG retrieves from docs index for documented topics | Added this iteration (`knowledge-base.test.ts`) | Passed — `retrieveKnowledge` returns docs-index snippets for documented topics |
| S12: Off-topic questions are still blocked | Added this iteration (`ai-rbse.test.ts`) | Passed — "who won the football match" is blocked |

## Notes

- The LiteLLM proxy at `http://10.10.80.31:4000/v1` was verified reachable from inside the Docker container; model `glm-5.2` confirmed in the proxy's model list.
- The docs index (`apps/applicant/data/docs-index.ts`) is auto-generated by `scripts/build-docs-index.mjs` and must not be edited manually. It is regenerated on every `npm run build` and Docker build.
- The `.env` file contains the LLM API key (`GLM_Z_API_KEY`) and is gitignored — never committed.
- The admin SidebarNav fix (`apps/admin/components/SidebarNav.{tsx,test.ts}`) remains uncommitted and is not part of this iteration.
- Manual testing was performed by the user (hitesh.kumar12@paysyslabs.com) against the running Docker container on `localhost:3100`.

## Verification

See `docs/plans/v0.20.6_AI_Mentor_UI_RAG_Context_Fix.md`.
