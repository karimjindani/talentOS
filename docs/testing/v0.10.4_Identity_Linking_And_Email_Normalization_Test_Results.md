# v0.10.4 — Identity Linking & Email Normalization — Test Results

Code version: `v0.10.4` · Date: `2026-07-02`

## Automated checks

| Command | Result |
|---|---|
| `npm run db:generate` | Passed |
| `npm run typecheck` | Passed (root + applicant + admin + ops) |
| `npm run lint` | Passed (both apps, `--max-warnings=0`) |
| `npm run test` | **65 tests passed** (62 prior + 3 new `users.test.ts`) |

New file `packages/db/src/users.test.ts` (3 tests) asserts `normalizeEmail`: lowercases + trims; is
idempotent; folds casing variants (`OWNER@acme.test` ≡ `owner@ACME.test`) to one identity.

## Data-layer verification (live Postgres)

Ran against the running DB (the `owner@acme.test` row created in earlier testing, with `keycloakSubjectId`
still NULL), then removed the scratch script:

| Check | Result |
|---|---|
| `getUserByEmail("OWNER@ACME.TEST")` resolves the row (case-insensitive) | **PASS** |
| `linkKeycloakIdentity` backfills the NULL `keycloakSubjectId` | **PASS** |
| Re-run with the same subject is a no-op | **PASS** |
| Unknown email returns `null` and is **not** created | **PASS** |
| No row created for the unknown email (count = 0) | **PASS** |

## Deployment

`docker compose up -d --build admin applicant` rebuilt and started cleanly (exit 0). Smoke: admin portal
`307` → Keycloak sign-in; applicant portal `200`. No migration (code-only change).

## Manual verification (recommended follow-up)

1. Sign in to the admin portal as a seeded admin (e.g. `hr@demo.talentos.local`) → on the next request the
   guard backfills their `keycloakSubjectId` (previously always NULL for admins).
2. Applicant status page (`/application`) resolves the user even if the session email casing differs from
   the stored row (previously showed "No application yet").
3. `session.user.isEmailVerified` reflects the Keycloak `email_verified` claim; no login is blocked by it.

## Notes

- No schema, migration, or data-model change. Regression suite: 65 tests.
- `email_verified` is exposed but not enforced (deferred until SMTP-backed verification).
