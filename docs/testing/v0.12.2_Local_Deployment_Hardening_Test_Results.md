# v0.12.2 Local Deployment Hardening Test Results

Code version: `v0.12.2`

Base commit before implementation: `7830833`

Date: `2026-07-04`

## Scope

Validate the hardened local deployment bootstrap, unified local OIDC issuer, stale-Keycloak repair path,
accepted-applicant dashboard seed and full login smoke tests.

## Results

| Check | Result | Notes |
| --- | --- | --- |
| `npm.cmd install` | Passed | Dependencies installed successfully. `npm audit` still reports 2 moderate findings; no force-fix was applied. |
| `docker compose config --quiet` | Passed | Compose configuration is valid. |
| `.env.example` browser hostname check | Passed | No browser-facing `host.docker.internal` remains in `.env.example` or `docker-compose.yml`. |
| `docker-compose.yml` local host mappings | Passed | App containers include `extra_hosts` for `keycloak.lvh.me:host-gateway` and `minio.lvh.me:host-gateway`. |
| Keycloak realm import check | Passed | Realm import includes current applicant/admin/Ops/provisioner clients, `lvh.me` redirect URIs, and accepted applicant demo user. |
| `npm.cmd run db:generate` | Passed | Prisma Client generated successfully. |
| `npm.cmd run typecheck` | Passed | Root, applicant, admin and Ops TypeScript checks passed. |
| `npm.cmd run test` | Passed | 25 test files passed; 133 tests passed. |
| `npm.cmd run build` | Passed | Applicant and Admin Next.js production builds completed successfully. |
| `npm.cmd run lint` | Passed | Applicant and Admin ESLint checks passed with zero warnings. Next.js plugin warning is pre-existing configuration noise. |
| `npm.cmd run local:bootstrap` | Passed | Repaired local `.env`, rebuilt app containers, ran Prisma generate/migrate/seed, patched existing Keycloak realm, seeded dashboard data, and started Ops Console. |
| `docker compose ps` | Passed | `talentos-postgres`, `talentos-keycloak-postgres`, `talentos-keycloak`, `talentos-minio`, `talentos-applicant`, and `talentos-admin` are running; database services are healthy. |
| `npm.cmd run local:doctor` | Passed | Applicant, Admin redirect, tenant Applicant, tenant Admin redirect, Keycloak discovery, MinIO health, and Ops Console checks passed. |
| `npm.cmd run local:smoke-login` | Passed | Full login callbacks passed for Org Admin, Applicant, Accepted Applicant dashboard, and Ops Console. |
| `git diff --check` | Passed | No whitespace errors. |

## Smoke Login Results

- `orgadmin@demo.talentos.local` reached `http://demo.lvh.me:3200/` with HTTP 200.
- `applicant@demo.talentos.local` reached `http://demo.lvh.me:3100/application` with HTTP 200.
- `accepted@demo.talentos.local` reached `http://demo.lvh.me:3100/dashboard` with HTTP 200.
- `orgadmin@demo.talentos.local` reached Ops Console at `http://127.0.0.1:3300/` with HTTP 200.

## Notes

- Bootstrap was validated against an existing local Keycloak volume and repaired the realm non-destructively.
- The previous issuer mismatch path was specifically covered by `npm.cmd run local:smoke-login`.
