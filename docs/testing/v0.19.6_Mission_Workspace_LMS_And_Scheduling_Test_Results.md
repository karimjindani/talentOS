# v0.19.6 Mission Workspace LMS, Curriculum Tooling And Thursday Scheduling Test Results

Date: 2026-07-23

## Summary

Verified the applicant Mission Workspace LMS redesign, admin curriculum tooling (top-level Tasks page,
document resources, prerequisite tasks), admin list pagination/filters and Overview KPIs, and the
mission scheduling changes (Thursday deadline cadence, repeat exclusion). All gate commands pass: the
full Vitest suite is green at **507 tests** (including new unit coverage and a fix for a stale-date
`submissions.test.ts` failure), and every regression area passes (the storage area carries its
pre-existing documented skip). Applicant and admin containers were rebuilt and verified healthy against
the current UI, and the user-guide screenshots were regenerated against it.

## Gate Commands

| Check | Result |
| --- | --- |
| `npm run typecheck` | Pass — `tsc --noEmit` clean for `apps/admin` and `apps/applicant` (which typecheck imported `@talentos/db`/`@talentos/ui`). |
| `npm run lint` | Pass — `eslint . --max-warnings=0` clean for `@talentos/admin` and `@talentos/applicant`. |
| `npm test` | Pass — 49 files, **507 tests**, 0 failures. |
| `npm run build` | Pass — `next build` compiles both `@talentos/admin` and `@talentos/applicant` (all routes). |
| `npm run regression:all` | Pass — missions 12/12, applicant 3/3, admin 4/4, dashboard 3/3, programs 2/2, auth 4/4, journal 4/4, tenant 6/6, ops 2/2, unit 507/507; storage carries 1 documented skip (CV upload/download scenario, pre-existing). |

## Scenario Results

| Scenario | Automation | Result |
| --- | --- | --- |
| S1: Mission Workspace derives steps, progress and submission mode | Added this iteration (`view-model.test.ts`) | Passed — 13 view-model cases (steps status, progress, continue target, countdown visibility, submission mode, canSubmit, reviewer feedback). |
| S2: Accepting a mission sets a Thursday deadline with ≥4 working days | Added this iteration (`regression:missions`, `mission-assignments.test.ts`) | Passed — 7 weekday `computeMissionDeadline` cases + accept test; Missions regression scenario asserts Thursday + ≥4 working days + grace after deadline. |
| S3: A repeat never re-serves a previously-assigned mission | Added/updated this iteration (`regression:missions`, `mission-assignments.test.ts`) | Passed — repeat test asserts `notIn` all prior missions; Missions repeat regressions green. |
| S4: Prerequisite tasks lock the mission's steps until complete | Added this iteration (`regression:missions`, `program-content.test.ts`); step-lock UI Deferred | Passed (data) — persistence + `listTasksByWeek` visibility verified; UI lock is a Known Gap. |
| S5: Document learning resources upload and download safely | Added this iteration (`program-content.test.ts`); route/UI Deferred | Passed (data) — DOCUMENT stores validated in-tenant `fileId`; missing/foreign file rejected. Download route + uploader are a Known Gap. |
| S6: YouTube learning resource gates completion at 90% watched | Deferred | Skipped in automation — client-only watch tracking; recorded as a Known Gap; manually verified in the rebuilt applicant container. |
| S7: Weekly learning tasks unlock sequentially in the workspace | Deferred | Skipped in automation — client-only ordering; Known Gap; manually verified. Underlying completion covered by existing task tests. |
| S8: Admin Tasks page manages weekly tasks + resources per program | Added this iteration (`program-content.test.ts`, `regression:programs`/`admin`) | Passed (data/actions) — reused audited content actions; collapsible/auto-collapse UI is a Known Gap; manually verified. |
| S9: Admin list pages paginate and filter | Added this iteration (`apps/admin/lib/pagination.test.ts`) | Passed — 10 cases (page-size clamping, slicing, page-window ellipsis, empty list). Page rendering is a Known Gap. |
| S10: Admin Overview reports live tenant counts | Deferred | Skipped in automation — read-only aggregation over already-tested list functions; Known Gap; manually verified against the rebuilt admin container. |

## Notes

- `submissions.test.ts` had 3 pre-existing failures caused by a hardcoded `2026-07-21/22` assignment
  deadline that had passed by the current date (2026-07-23); fixed by making the default deadline
  relative to `now`, so those tests are date-independent going forward.
- Two Prisma migrations were applied to the local database and the client regenerated:
  `20260723120000_learning_resource_document`, `20260723140000_program_task_prerequisite`.
- Deferred client-only scenarios (S6, S7, S10, and the UI portions of S4/S5/S8/S9) are recorded as
  **Known Gaps** in `docs/Regression_Scenarios.md`; the node/Vitest environment renders no DOM, so these
  need a future jsdom/browser harness.

## Verification

See `docs/plans/v0.19.6_Mission_Workspace_LMS_And_Scheduling.md`.
