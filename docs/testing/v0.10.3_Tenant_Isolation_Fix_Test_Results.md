# v0.10.3 — Tenant Isolation Fix — Test Results

Code version: `v0.10.3` · Date: `2026-07-02`

## Automated checks

| Command | Result |
|---|---|
| `npm run db:generate` | Passed (Prisma client regenerated) |
| `npm run typecheck` | Passed (root + applicant + admin + ops projects) |
| `npm run lint` | Passed (both apps, `--max-warnings=0`) |
| `npm run test` | **62 tests passed** (56 prior + 6 new `permissions.test.ts`) |

New file `packages/auth/src/permissions.test.ts` (6 tests) asserts `tenantRolesGrant`:
ORG_ADMIN → programs/review/settings/users; HR → review only; TECH_LEAD → evaluate only (no review/
settings); APPLICANT → none; **empty roles → deny everything** (the cross-tenant actor with no
membership); and capability union across multiple roles.

## Data-layer isolation verification (live Postgres)

`getActorTenantRoles` is the sole input to the guard's decision. Verified directly against the running
DB using the `demo` and `acme` tenants and the `owner@acme.test` ORG_ADMIN provisioned during the earlier
end-to-end test:

| Case | Result |
|---|---|
| acme ORG_ADMIN on **acme** | `ORG_ADMIN` → **ALLOW** ✅ |
| acme ORG_ADMIN on **demo** | `(none)` → **DENY** ✅ (previously allowed) |
| `OWNER@ACME.TEST` on acme (case-insensitive) | `ORG_ADMIN` → **ALLOW** ✅ |
| demo HR on **demo** | `HR` → **ALLOW** (review) ✅ |
| demo HR on **acme** | `(none)` → **DENY** ✅ (cross-tenant blocked) |

All five cases passed, confirming a realm-wide role no longer authorizes actions outside the actor's
tenant membership, while same-tenant access and case-insensitive matching are preserved.

## Deployment

`docker compose up -d --build admin` rebuilt and started cleanly (exit 0); the admin portal responds
`307` → Keycloak sign-in. No migration (code-only change).

## Manual verification (recommended follow-up in a browser)

1. Sign in to `acme.localhost:3200` as `owner@acme.test` → can view/manage acme programs, branding.
2. Navigate to `localhost:3200` (demo) or another tenant subdomain → **"Access denied"** notice; program
   and branding writes rejected; `GET /api/files/<demo-file-id>/download` → 403/404.
3. `superadmin` → still reaches any tenant. Demo `hr@`/`techlead@` → still work on demo only.

## Notes

- No schema, migration, or data-model change. Regression suite: 62 tests.
- The Keycloak realm role now gates only portal entry; per-tenant authority comes from `TenantMembership`.
