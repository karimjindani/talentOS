# v0.1.2 Architecture Engineering Backlog Update Plan

Code version: `v0.1.2`

Previous baseline: `v0.1.1`

Previous baseline commit: `3cb7dcf78e56a9c916a02216763e0c0f2cd27c32`

Plan status: Implemented

## Summary

Create a documentation-only patch update that adds an Engineering To-Do List to the architecture documentation and records two major engineering backlog decisions:

- IAM should use Keycloak.
- Admin and Applicant portals should become separate portal surfaces.

## Documentation Changes

- Update `docs/Architecture.md` with:
  - code version `v0.1.2`,
  - target runtime diagram,
  - portal separation direction,
  - Keycloak IAM direction,
  - Engineering To-Do List mapped from Product Backlog.

- Update `docs/Product_Backlog.md` with:
  - Engineering Backlog section,
  - Keycloak as a major platform item,
  - separate Admin and Applicant portals as a major platform item.

- Update `docs/Decision_Log.md` with:
  - Keycloak IAM decision,
  - portal separation decision,
  - architecture-level Engineering To-Do tracking decision.

- Update `docs/Version_Baseline.md` with:
  - current documentation version `v0.1.2`,
  - note that this is a documentation-only update.

## No Runtime Changes

This update does not change application code, database schema, Docker runtime behavior or package versions.

## Validation

- Run `git diff --check`.
- Confirm required plan and test-result Markdown files exist.
- Confirm `Architecture.md` contains:
  - `Engineering To-Do List`,
  - `Keycloak`,
  - `Separate Applicant Portal`,
  - `Separate Admin Portal`.
