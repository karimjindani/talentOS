# v0.15.0 AI Mentor Roadmap

## Summary

Deliver a context-aware AI Mentor that guides applicants throughout their internship rather than
functioning as a generic chatbot. The mentor is built in ten incremental phases — each phase is
independently shippable, tested, and documented per the SSDLC rules in [`sdlc.md`](../sdlc.md).

Current version baseline: `v0.14.2` (Applicant Portal Tenant Isolation, D-065).

Branch: `feat/applicant-ai-mentor-skeleton`.

## SSDLC Compliance

This roadmap follows the principles defined in [`sdlc.md`](../sdlc.md):

| Principle | How this roadmap complies |
|---|---|
| 0 — Document what you do | This plan lives in `docs/plans/`. Each phase updates it with status and results. |
| 1 — No regressions | Every phase runs the full test suite + regression suite before merge. |
| 2 — Architecture doc updated | `docs/Architecture.md` updated when the API layer (Phase 2) and DB model (Phase 3) land. |
| 3 — Testing per iteration | Each phase has a Test Plan section; regression suite updated as needed. |
| 6 — Data model updated | `docs/Data_Model.md` and `docs/Data_Dictionary.md` updated in Phase 3 (Chat History). |
| 7 — Security from iteration 1 | Tenant isolation, auth guard, and request validation are present from Phase 1–2. |

### Version & Documentation Control

- Plan stored as Markdown in `docs/plans/` ✅ (this file).
- Testing details recorded in `docs/testing/` per phase.
- Version baseline recorded in `docs/Version_Baseline.md` when `v0.15.0` is tagged.
- User-facing portal changes update `docs/user-guides/` (Phase 1 nav change, Phase 4 context questions).

### Source Control

- Branch: `feat/applicant-ai-mentor-skeleton` (trunk-based, short-lived).
- Commits follow Conventional Commits with `(v0.15.0, D-0NN)` trailers.
- Every phase lands via PR — no direct pushes to `main`.

## Phase Status Overview

| Phase | Title | Status | Version |
|---|---|---|---|
| 1 | AI Mentor Chatbox UI | ✅ Complete | `v0.15.0` |
| 2 | AI Mentor API Stub | ✅ Complete | `v0.15.0` |
| 3 | Chat History | ✅ Complete | `v0.15.1` |
| 4 | AI Context Integration | ⬜ Not Started | `v0.15.2` |
| 5 | Knowledge Base Integration | ⬜ Not Started | `v0.15.3` |
| 6 | Real LLM Integration | ⬜ Not Started | `v0.16.0` |
| 7 | Smart Mentor Features | ⬜ Not Started | `v0.16.1` |
| 8 | Admin AI Analytics | ⬜ Not Started | `v0.16.2` |
| 9 | Assignment Assistance | ⬜ Not Started | `v0.17.0` |
| 10 | AI Mentor v1.0 | ⬜ Not Started | `v1.0.0` |

**Legend:** ✅ Complete · 🔄 In Progress · ⬜ Not Started · ⏸️ Deferred

---

## Phase 1: AI Mentor Chatbox UI ✅

**Goal:** Establish the complete frontend experience.

### Includes

- AI Mentor page at `/dashboard/mentor`
- Sidebar navigation link in `ApplicantShell`
- Chat interface with message history
- Input field and Send button
- Hardcoded mentor responses (frontend only)
- Responsive and professional UI using existing Tailwind brand classes

### Does Not Include

- Backend / API calls
- AI integration
- Database models
- Conversation persistence

### Key Changes

- `apps/applicant/components/ApplicantShell.tsx` — added `{ href: "/dashboard/mentor", label: "AI Mentor" }` to `APPLICANT_NAV_ITEMS`.
- `apps/applicant/app/dashboard/mentor/page.tsx` — new client component with message state, input, send button, hardcoded dummy response.
- `apps/applicant/components/ApplicantShell.test.ts` — updated nav item count (8→9), added mentor route assertions.

### Test Plan

- Unit: `ApplicantShell.test.ts` — 12 tests pass (nav item count, route matching, completeness).
- Typecheck: `npx tsc --noEmit -p apps/applicant/tsconfig.json` — passes.
- Manual: `http://demo.lvh.me:3100/dashboard/mentor` — page loads, chat UI renders, send produces dummy response.
- Docker: `docker compose build applicant && docker compose up -d applicant` — route served on port 3100.

### Status

✅ **Complete** — all tests pass, typecheck clean, Docker image rebuilt and verified.

---

## Phase 2: AI Mentor API Stub ✅

**Goal:** Prepare the architecture for AI integration by moving dummy responses to a backend API layer.

### Includes

- AI Mentor API endpoint (`/api/ai/mentor`)
- Chat requests sent to backend via `fetch`
- Structured dummy responses (text + rich cards)
- Request validation (non-empty prompt, max length)
- Error handling (400 for invalid input, 500 for server errors)
- Loading state in the frontend

### Does Not Include

- Real AI / LLM calls
- Database persistence
- Conversation history

### Key Changes

- `apps/applicant/lib/ai.ts` — enhance `AIInteractionResponse` with `MentorCard[]` type; keyword-based structured dummy responses (task, progress, timeline, tips, badge cards).
- `apps/applicant/app/api/ai/mentor/route.ts` — add request validation (prompt required, max 2000 chars), error handling, structured JSON response.
- `apps/applicant/app/dashboard/mentor/page.tsx` — replace `setTimeout` stub with `fetch("/api/ai/mentor")`, handle loading and error states, render rich cards.

### Test Plan

- Unit: API route validation tests (empty prompt → 400, valid prompt → 200 with structured response).
- Unit: `lib/ai.ts` keyword matching returns correct card types.
- Unit: Frontend loading state and error boundary.
- Typecheck: `npx tsc --noEmit -p apps/applicant/tsconfig.json`.
- Manual: Send messages containing "task", "progress", "timeline", "tips" — verify correct card rendering.

### Status

✅ **Complete** — typecheck clean, 12/12 nav tests pass, API stub with structured responses, validation, and error handling implemented.

---

## Phase 3: Chat History ✅

**Goal:** Make conversations persistent.

### Includes

- Database model (`MentorConversation`, `MentorMessage`)
- Save conversations after each exchange
- Load previous chats on page visit
- User-specific history (scoped to `userId`)
- Tenant isolation (scoped to `tenantId`)
- Conversation timestamps

### Does Not Include

- Real AI
- Mission context

### Key Changes

- `packages/db/prisma/schema.prisma` — add `MentorConversation` and `MentorMessage` models.
- `packages/db/prisma/migrations/` — new migration.
- `docs/Data_Model.md` — update with new models.
- `docs/Data_Dictionary.md` — update with field definitions.
- `apps/applicant/app/api/ai/mentor/route.ts` — persist messages to DB.
- `apps/applicant/app/dashboard/mentor/page.tsx` — load conversation history on mount.

### Test Plan

- Unit: Conversation save/load round-trip.
- Unit: Tenant isolation — user A cannot read user B's conversations.
- Unit: Messages ordered by timestamp.
- Regression: `regression:mentor` scenario — chat history survives page reload.
- Required: `npm run db:generate`, `npm run test`, `npm run build`.

### Status

✅ **Complete** — Prisma models added, migration applied, API persists and loads conversation history, typecheck clean, 12/12 nav tests pass.

---

## Phase 4: AI Context Integration ⬜

**Goal:** Make responses personalized instead of generic.

### Includes

- Current internship week
- Current progress percentage
- Upcoming tasks
- Submission status
- Internship timeline
- Applicant profile context

### Example Questions

- "What is my task today?"
- "Show my progress."
- "What's due this week?"
- "Show my internship timeline."

### Key Changes

- `apps/applicant/lib/ai-context.ts` — new module to gather applicant context (missions, submissions, program week).
- `apps/applicant/app/api/ai/mentor/route.ts` — inject context into the AI interaction request.
- `apps/applicant/lib/ai.ts` — use context to generate personalized dummy responses.
- `docs/user-guides/` — update applicant guide with example questions.

### Test Plan

- Unit: Context builder returns correct week, progress, upcoming tasks.
- Unit: Personalized responses reference real applicant data.
- Regression: Context is tenant-scoped and user-scoped.

### Status

⬜ **Not Started** — planned for `v0.15.2`.

---

## Phase 5: Knowledge Base Integration ⬜

**Goal:** AI answers using project knowledge instead of general knowledge.

### Includes

- SDLC documents
- Internship guide
- Mission documentation
- Engineering process
- FAQs
- Learning resources

### Example Questions

- "Explain SDLC."
- "How do I write a PRD?"
- "What is SEM?"

### Key Changes

- `packages/db/prisma/schema.prisma` — `KnowledgeDocument` model (or file-based index).
- `apps/applicant/lib/knowledge-base.ts` — document search/retrieval module.
- `apps/applicant/app/api/ai/mentor/route.ts` — retrieve relevant docs before responding.
- Seed: Index `docs/` content into the knowledge base.

### Test Plan

- Unit: Document search returns relevant results for SDLC/SEM/PRD queries.
- Unit: Knowledge base is read-only for applicants.
- Regression: Knowledge responses cite source documents.

### Status

⬜ **Not Started** — planned for `v0.15.3`.

---

## Phase 6: Real LLM Integration ⬜

**Goal:** Enable real AI-powered mentoring.

### Includes

- LiteLLM / OpenAI / GLM integration
- Prompt templates
- System prompts
- Context injection
- Response streaming
- Token management

### Key Changes

- `packages/ai/` — new shared package for LLM client, prompt templates, token management.
- `apps/applicant/app/api/ai/mentor/route.ts` — streaming response via Server-Sent Events.
- `apps/applicant/app/dashboard/mentor/page.tsx` — streaming message rendering.
- `.env` — LLM API key configuration (never committed).

### Test Plan

- Unit: Prompt template construction with context injection.
- Unit: Streaming response parsing.
- Integration: LLM API call with mock responses.
- Security: API key never exposed to client; rate limiting.

### Status

⬜ **Not Started** — planned for `v0.16.0`.

---

## Phase 7: Smart Mentor Features ⬜

**Goal:** Make the mentor proactive.

### Includes

- Task recommendations
- Learning suggestions
- Daily guidance
- Progress analysis
- Next-step recommendations
- Weekly summaries

### Example

> "You're 60% through Week 2. Complete Task 4 before Friday."

### Key Changes

- `apps/applicant/lib/ai-proactive.ts` — proactive recommendation engine.
- Scheduled job or on-login hook to generate daily guidance.
- Dashboard notification integration.

### Test Plan

- Unit: Recommendation engine produces correct suggestions based on progress.
- Unit: Weekly summary generation.
- Regression: Proactive messages do not spam (rate-limited).

### Status

⬜ **Not Started** — planned for `v0.16.1`.

---

## Phase 8: Admin AI Analytics ⬜

**Goal:** Allow admins to monitor AI usage.

### Includes

- AI usage dashboard
- Conversation statistics
- Popular questions
- AI activity logs
- Token usage
- Feedback analytics

### Key Changes

- `apps/admin/app/operations/ai-analytics/` — new admin analytics page.
- `packages/db/prisma/schema.prisma` — `AIMentorInteraction` audit model.
- Admin capability: `viewAIAnalytics`.

### Test Plan

- Unit: Analytics aggregation queries.
- Unit: Only `SUPER_ADMIN` / `ORG_ADMIN` can access analytics.
- Regression: Analytics page is tenant-scoped.

### Status

⬜ **Not Started** — planned for `v0.16.2`.

---

## Phase 9: Assignment Assistance ⬜

**Goal:** Guide interns without completing their work.

### Includes

- PRD review
- Code review guidance
- Testing checklist
- Deployment checklist
- Submission hints
- Engineering feedback

### Key Changes

- `apps/applicant/app/api/ai/mentor/route.ts` — submission-aware context injection.
- `apps/applicant/lib/ai-review.ts` — review guidance generator (rubric-based, not solution-giving).
- Integration with mission submissions and PRD drafts.

### Test Plan

- Unit: Review guidance references submission content.
- Unit: Mentor never provides complete solutions (guardrail test).
- Regression: Assignment assistance respects submission deadlines.

### Status

⬜ **Not Started** — planned for `v0.17.0`.

---

## Phase 10: AI Mentor v1.0 ⬜

**Goal:** Deliver a context-aware AI Mentor that guides applicants throughout their internship.

### Includes

- Full internship context
- Knowledge Base
- Mission awareness
- Progress tracking
- Personalized mentoring
- Conversation history
- Admin analytics
- Secure multi-tenant support
- Optimized prompts
- Production-ready architecture

### Key Changes

- Consolidate all phases 1–9 into a production-ready feature.
- Performance optimization (response caching, lazy loading).
- Security hardening (rate limiting, input sanitization, PII redaction).
- Documentation: `docs/Architecture.md`, `docs/user-guides/`, `docs/Deployment.md` finalized.

### Test Plan

- Full regression suite: `npm run regression:all`.
- Load testing for streaming responses.
- Security audit: tenant isolation, auth, rate limits.
- User acceptance testing with demo tenant.

### Status

⬜ **Not Started** — planned for `v1.0.0`.

---

## Assumptions

- Phase 1 is complete and merged on branch `feat/applicant-ai-mentor-skeleton`.
- Each phase is a separate PR; no phase is merged without passing CI and review.
- Database schema changes (Phase 3+) require migration + data model doc updates per SSDLC.
- LLM integration (Phase 6+) requires an API key configured via environment variable; no keys are committed.
- The AI Mentor is applicant-portal only; admin analytics (Phase 8) is in the admin portal.
- All phases respect existing tenant isolation and auth guards.

## Open Questions

- Which LLM provider for Phase 6? (OpenAI, GLM, or LiteLLM router?)
- Should conversation history (Phase 3) have a retention policy / auto-delete?
- Should the knowledge base (Phase 5) be file-based or DB-indexed?
- Rate limiting strategy for Phase 6+ (per-user, per-tenant, global)?
